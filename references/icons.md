# Icon Library（本地图标库）

> AUTO-GENERATED（scripts/gen-icons-doc.mjs）——修改图标库后重新生成。

## 用法

`iconName` 格式为 `style:name`：

| 前缀 | 语义 | 说明 |
|---|---|---|
| `bs:` | 本地库直引 | 下方清单中的任意 name，如 `bs:rocket` |
| `fas:` | Font Awesome Solid | 按 FA 语义名映射到本地近似图标，如 `fas:house`；仅下表覆盖的 FA 名可用 |
| `far:` | Font Awesome Regular | 同 `fas:` 映射表（Regular 语义无区分） |
| `fab:` | Font Awesome Brands | **不支持**——本地库无品牌 logo（版权）；品牌标识请用图片元素 |

> 生成时优先用 `bs:` 直引（一定存在）；用 `fas:` 时先查下方映射表确认存在，否则导出会跳过该图标。

## 本地图标库（192 个，按分类）

### 方向/箭头（20）

| name | 中文 |
|---|---|
| `bs:arrow-down` | 下箭头 |
| `bs:arrow-down-circle` | 下箭头圆 |
| `bs:arrow-down-right` | 斜向下箭头 |
| `bs:arrow-left` | 左箭头 |
| `bs:arrow-left-circle` | 左箭头圆 |
| `bs:arrow-repeat` | 循环 |
| `bs:arrow-right` | 右箭头 |
| `bs:arrow-right-circle` | 右箭头圆 |
| `bs:arrow-up` | 上箭头 |
| `bs:arrow-up-circle` | 上箭头圆 |
| `bs:arrow-up-right` | 右上箭头 |
| `bs:arrows-collapse` | 向内收缩 |
| `bs:arrows-expand` | 向外展开 |
| `bs:box-arrow-up-right` | 外链 |
| `bs:chevron-down` | 下尖括号 |
| `bs:chevron-left` | 左尖括号 |
| `bs:chevron-right` | 右尖括号 |
| `bs:chevron-up` | 上尖括号 |
| `bs:forward` | 快进 |
| `bs:reply` | 回复 |

### 状态/提示（16）

| name | 中文 |
|---|---|
| `bs:ban` | 禁止 |
| `bs:check` | 对勾 |
| `bs:check-circle` | 对勾圆圈 |
| `bs:check-circle-fill` | 对勾圆圈实心 |
| `bs:check-lg` | 大对勾 |
| `bs:check2-all` | 全部完成 |
| `bs:dash` | 横杠 |
| `bs:exclamation-circle` | 感叹圆圈 |
| `bs:exclamation-triangle` | 警告三角 |
| `bs:info-circle` | 信息圆圈 |
| `bs:plus` | 加号 |
| `bs:plus-lg` | 大加号 |
| `bs:question-circle` | 问号圆圈 |
| `bs:x` | 叉号 |
| `bs:x-circle` | 叉号圆圈 |
| `bs:x-lg` | 大叉号 |

### 概念/象征（26）

| name | 中文 |
|---|---|
| `bs:award` | 奖章 |
| `bs:bullseye` | 靶心 |
| `bs:cloud` | 云朵 |
| `bs:cloud-fill` | 云朵实心 |
| `bs:droplet` | 水滴 |
| `bs:eye` | 眼睛 |
| `bs:eye-slash` | 闭眼 |
| `bs:fire` | 火焰 |
| `bs:flag` | 旗帜 |
| `bs:flag-fill` | 旗帜实心 |
| `bs:flower1` | 花朵 |
| `bs:gem` | 宝石 |
| `bs:heart` | 心形 |
| `bs:heart-fill` | 心形实心 |
| `bs:lightbulb` | 灯泡 |
| `bs:lightbulb-fill` | 灯泡实心 |
| `bs:moon` | 月亮 |
| `bs:moon-stars` | 星月 |
| `bs:rocket` | 火箭 |
| `bs:rocket-takeoff` | 火箭发射 |
| `bs:star` | 星形 |
| `bs:star-fill` | 星形实心 |
| `bs:star-half` | 半星 |
| `bs:sun` | 太阳 |
| `bs:trophy` | 奖杯 |
| `bs:trophy-fill` | 奖杯实心 |

### 文档/文件（12）

| name | 中文 |
|---|---|
| `bs:archive` | 归档箱 |
| `bs:bookmark-star` | 书签星标 |
| `bs:clipboard-check` | 剪贴板对勾 |
| `bs:clipboard-data` | 剪贴板数据 |
| `bs:file-earmark-pdf` | PDF 文档 |
| `bs:file-earmark-text` | 文档带角 |
| `bs:file-spreadsheet` | 表格文档 |
| `bs:file-text` | 文档 |
| `bs:files` | 多文件 |
| `bs:folder` | 文件夹 |
| `bs:folder2-open` | 打开文件夹 |
| `bs:journal-text` | 日志 |

### 图表/数据（13）

| name | 中文 |
|---|---|
| `bs:activity` | 脉搏 |
| `bs:bar-chart` | 柱状图 |
| `bs:bar-chart-line` | 柱线图 |
| `bs:bar-chart-steps` | 阶梯图 |
| `bs:funnel` | 漏斗 |
| `bs:graph-down` | 下降折线 |
| `bs:graph-up` | 上升折线 |
| `bs:graph-up-arrow` | 上升箭头图 |
| `bs:infinity` | 无限 |
| `bs:lightning-charge` | 闪电 |
| `bs:lightning-charge-fill` | 闪电实心 |
| `bs:pie-chart` | 饼图 |
| `bs:speedometer` | 速度表 |

### 财务/商业（14）

| name | 中文 |
|---|---|
| `bs:bank` | 银行 |
| `bs:calculator` | 计算器 |
| `bs:cash` | 现金 |
| `bs:cash-stack` | 现金堆 |
| `bs:coin` | 硬币 |
| `bs:credit-card` | 信用卡 |
| `bs:currency-dollar` | 美元 |
| `bs:currency-euro` | 欧元 |
| `bs:currency-exchange` | 汇率 |
| `bs:currency-yen` | 日元 |
| `bs:percent` | 百分号 |
| `bs:piggy-bank` | 存钱罐 |
| `bs:safe` | 保险箱 |
| `bs:wallet` | 钱包 |

### 工具/操作（24）

| name | 中文 |
|---|---|
| `bs:box` | 盒子 |
| `bs:box-seam` | 包裹 |
| `bs:cloud-arrow-down` | 云端下载 |
| `bs:cloud-arrow-up` | 云端上传 |
| `bs:download` | 下载 |
| `bs:gear` | 齿轮 |
| `bs:gear-fill` | 齿轮实心 |
| `bs:gear-wide-connected` | 齿轮连线 |
| `bs:grid` | 网格 |
| `bs:hammer` | 锤子 |
| `bs:key` | 钥匙 |
| `bs:layers` | 图层 |
| `bs:link-45deg` | 链接 |
| `bs:list-check` | 清单 |
| `bs:list-ul` | 列表 |
| `bs:pencil` | 铅笔 |
| `bs:search` | 搜索 |
| `bs:share` | 分享 |
| `bs:sliders` | 调节滑杆 |
| `bs:tools` | 工具套装 |
| `bs:trash` | 垃圾桶 |
| `bs:trash-fill` | 垃圾桶实心 |
| `bs:upload` | 上传 |
| `bs:wrench` | 扳手 |

### 设备/硬件（17）

| name | 中文 |
|---|---|
| `bs:battery-full` | 满电 |
| `bs:camera` | 相机 |
| `bs:cpu` | 芯片 |
| `bs:display` | 显示器 |
| `bs:images` | 图片集 |
| `bs:keyboard` | 键盘 |
| `bs:laptop` | 笔记本 |
| `bs:music-note` | 音符 |
| `bs:pause-circle` | 暂停 |
| `bs:phone` | 手机 |
| `bs:play-circle` | 播放 |
| `bs:printer` | 打印机 |
| `bs:stop-circle` | 停止 |
| `bs:tablet` | 平板 |
| `bs:usb-drive` | U 盘 |
| `bs:volume-up` | 音量 |
| `bs:wifi` | 无线 |

### 沟通/媒体（12）

| name | 中文 |
|---|---|
| `bs:bell` | 铃铛 |
| `bs:bell-fill` | 铃铛实心 |
| `bs:chat-dots` | 对话气泡 |
| `bs:chat-dots-fill` | 对话气泡实心 |
| `bs:chat-square-text` | 文字气泡 |
| `bs:envelope` | 邮件 |
| `bs:envelope-fill` | 邮件实心 |
| `bs:megaphone` | 喇叭 |
| `bs:mic` | 麦克风 |
| `bs:send` | 发送 |
| `bs:telephone` | 电话 |
| `bs:telephone-fill` | 电话实心 |

### 时间/日程（9）

| name | 中文 |
|---|---|
| `bs:alarm` | 闹钟 |
| `bs:calendar` | 日历 |
| `bs:calendar-check` | 日历对勾 |
| `bs:calendar-date` | 日历日期 |
| `bs:calendar-event` | 日历事件 |
| `bs:clock` | 时钟 |
| `bs:clock-history` | 历史时钟 |
| `bs:hourglass-split` | 沙漏 |
| `bs:stopwatch` | 秒表 |

### 位置/地图（11）

| name | 中文 |
|---|---|
| `bs:building` | 大楼 |
| `bs:compass` | 罗盘 |
| `bs:door-open` | 开门 |
| `bs:geo-alt` | 定位针 |
| `bs:geo-alt-fill` | 定位针实心 |
| `bs:globe` | 地球 |
| `bs:globe-americas` | 美洲地球 |
| `bs:house` | 房屋 |
| `bs:house-fill` | 房屋实心 |
| `bs:map` | 地图 |
| `bs:pin-map` | 地图针 |

### 安全/隐私（7）

| name | 中文 |
|---|---|
| `bs:fingerprint` | 指纹 |
| `bs:lock` | 锁 |
| `bs:lock-fill` | 锁实心 |
| `bs:shield-check` | 盾牌对勾 |
| `bs:shield-lock` | 盾牌锁 |
| `bs:shield-shaded` | 盾牌 |
| `bs:unlock` | 开锁 |

### 人员/用户（11）

| name | 中文 |
|---|---|
| `bs:hand-thumbs-down` | 点踩 |
| `bs:hand-thumbs-up` | 点赞 |
| `bs:headset` | 耳麦 |
| `bs:people` | 多人 |
| `bs:people-fill` | 多人实心 |
| `bs:person` | 单人 |
| `bs:person-badge` | 人员徽章 |
| `bs:person-check` | 人员对勾 |
| `bs:person-circle` | 人员头像 |
| `bs:person-fill` | 单人实心 |
| `bs:person-plus` | 添加人员 |

## Font Awesome → 本地映射（1098 条）

| FA 名 | 用法 | 本地图标 |
|---|---|---|
| activity | `fas:activity` / `far:activity` | `bs:activity`（脉搏） |
| activity-regular | `fas:activity-regular` / `far:activity-regular` | `bs:activity`（脉搏） |
| address-book | `fas:address-book` / `far:address-book` | `bs:journal-text`（日志） |
| address-card | `fas:address-card` / `far:address-card` | `bs:person-vcard`（） |
| airplane | `fas:airplane` / `far:airplane` | `bs:send`（发送） |
| alarm-clock | `fas:alarm-clock` / `far:alarm-clock` | `bs:alarm`（闹钟） |
| alarm-clock-regular | `fas:alarm-clock-regular` / `far:alarm-clock-regular` | `bs:alarm`（闹钟） |
| anchor | `fas:anchor` / `far:anchor` | `bs:anchor`（） |
| anchor-circle-check | `fas:anchor-circle-check` / `far:anchor-circle-check` | `bs:anchor`（） |
| anchor-circle-exclamation | `fas:anchor-circle-exclamation` / `far:anchor-circle-exclamation` | `bs:anchor`（） |
| anchor-circle-xmark | `fas:anchor-circle-xmark` / `far:anchor-circle-xmark` | `bs:anchor`（） |
| anchor-lock | `fas:anchor-lock` / `far:anchor-lock` | `bs:anchor`（） |
| anchor-regular-regular | `fas:anchor-regular-regular` / `far:anchor-regular-regular` | `bs:anchor`（） |
| angle-down | `fas:angle-down` / `far:angle-down` | `bs:chevron-down`（下尖括号） |
| angle-left | `fas:angle-left` / `far:angle-left` | `bs:chevron-left`（左尖括号） |
| angle-right | `fas:angle-right` / `far:angle-right` | `bs:chevron-right`（右尖括号） |
| angle-up | `fas:angle-up` / `far:angle-up` | `bs:chevron-up`（上尖括号） |
| apple-regular | `fas:apple-regular` / `far:apple-regular` | `bs:apple`（） |
| apple-whole | `fas:apple-whole` / `far:apple-whole` | `bs:apple`（） |
| archive | `fas:archive` / `far:archive` | `bs:archive`（归档箱） |
| arrow-down | `fas:arrow-down` / `far:arrow-down` | `bs:arrow-down`（下箭头） |
| arrow-down-long | `fas:arrow-down-long` / `far:arrow-down-long` | `bs:arrow-down`（下箭头） |
| arrow-down-to-bracket | `fas:arrow-down-to-bracket` / `far:arrow-down-to-bracket` | `bs:box-arrow-in-down`（） |
| arrow-down-to-water | `fas:arrow-down-to-water` / `far:arrow-down-to-water` | `bs:cloud-arrow-down`（云端下载） |
| arrow-left | `fas:arrow-left` / `far:arrow-left` | `bs:arrow-left`（左箭头） |
| arrow-left-from-bracket | `fas:arrow-left-from-bracket` / `far:arrow-left-from-bracket` | `bs:box-arrow-left`（） |
| arrow-left-long | `fas:arrow-left-long` / `far:arrow-left-long` | `bs:arrow-left`（左箭头） |
| arrow-pointer | `fas:arrow-pointer` / `far:arrow-pointer` | `bs:cursor`（） |
| arrow-right | `fas:arrow-right` / `far:arrow-right` | `bs:arrow-right`（右箭头） |
| arrow-right-long | `fas:arrow-right-long` / `far:arrow-right-long` | `bs:arrow-right`（右箭头） |
| arrow-right-to-bracket | `fas:arrow-right-to-bracket` / `far:arrow-right-to-bracket` | `bs:box-arrow-in-right`（） |
| arrow-rotate-left | `fas:arrow-rotate-left` / `far:arrow-rotate-left` | `bs:arrow-counterclockwise`（） |
| arrow-rotate-right | `fas:arrow-rotate-right` / `far:arrow-rotate-right` | `bs:arrow-clockwise`（） |
| arrow-trend-down | `fas:arrow-trend-down` / `far:arrow-trend-down` | `bs:graph-down`（下降折线） |
| arrow-trend-up | `fas:arrow-trend-up` / `far:arrow-trend-up` | `bs:graph-up-arrow`（上升箭头图） |
| arrow-up | `fas:arrow-up` / `far:arrow-up` | `bs:arrow-up`（上箭头） |
| arrow-up-from-bracket | `fas:arrow-up-from-bracket` / `far:arrow-up-from-bracket` | `bs:box-arrow-up`（） |
| arrow-up-long | `fas:arrow-up-long` / `far:arrow-up-long` | `bs:arrow-up`（上箭头） |
| arrows-left-right | `fas:arrows-left-right` / `far:arrows-left-right` | `bs:arrow-left-right`（） |
| arrows-rotate | `fas:arrows-rotate` / `far:arrows-rotate` | `bs:arrow-repeat`（循环） |
| arrows-spin | `fas:arrows-spin` / `far:arrows-spin` | `bs:arrow-repeat`（循环） |
| arrows-turn-to-dots | `fas:arrows-turn-to-dots` / `far:arrows-turn-to-dots` | `bs:arrow-repeat`（循环） |
| arrows-up-down | `fas:arrows-up-down` / `far:arrows-up-down` | `bs:arrow-up-down`（） |
| arrows-up-down-left-right | `fas:arrows-up-down-left-right` / `far:arrows-up-down-left-right` | `bs:arrows-move`（） |
| asterisk | `fas:asterisk` / `far:asterisk` | `bs:asterisk`（） |
| at | `fas:at` / `far:at` | `bs:at`（） |
| atom | `fas:atom` / `far:atom` | `bs:atom`（） |
| austral-sign | `fas:austral-sign` / `far:austral-sign` | `bs:currency-dollar`（美元） |
| award | `fas:award` / `far:award` | `bs:award`（奖章） |
| baby | `fas:baby` / `far:baby` | `bs:baby`（） |
| baby-carriage | `fas:baby-carriage` / `far:baby-carriage` | `bs:baby`（） |
| backward | `fas:backward` / `far:backward` | `bs:rewind`（） |
| backward-step | `fas:backward-step` / `far:backward-step` | `bs:skip-start`（） |
| bacon | `fas:bacon` / `far:bacon` | `bs:egg`（） |
| bag-shopping | `fas:bag-shopping` / `far:bag-shopping` | `bs:bag`（） |
| baht-sign | `fas:baht-sign` / `far:baht-sign` | `bs:currency-dollar`（美元） |
| balance-scale | `fas:balance-scale` / `far:balance-scale` | `bs:bank`（银行） |
| ban-regular | `fas:ban-regular` / `far:ban-regular` | `bs:ban`（禁止） |
| bandage | `fas:bandage` / `far:bandage` | `bs:bandage`（） |
| bandage-regular | `fas:bandage-regular` / `far:bandage-regular` | `bs:bandage`（） |
| bank | `fas:bank` / `far:bank` | `bs:bank`（银行） |
| barcode | `fas:barcode` / `far:barcode` | `bs:upc-scan`（） |
| bars | `fas:bars` / `far:bars` | `bs:list`（） |
| bars-staggered | `fas:bars-staggered` / `far:bars-staggered` | `bs:list-check`（清单） |
| baseball | `fas:baseball` / `far:baseball` | `bs:trophy`（奖杯） |
| baseball-regular | `fas:baseball-regular` / `far:baseball-regular` | `bs:trophy`（奖杯） |
| basketball | `fas:basketball` / `far:basketball` | `bs:trophy`（奖杯） |
| basketball-regular | `fas:basketball-regular` / `far:basketball-regular` | `bs:trophy`（奖杯） |
| bath | `fas:bath` / `far:bath` | `bs:emoji-laughing`（） |
| bath-regular | `fas:bath-regular` / `far:bath-regular` | `bs:emoji-laughing`（） |
| battery-bolt | `fas:battery-bolt` / `far:battery-bolt` | `bs:battery-charging`（） |
| battery-empty | `fas:battery-empty` / `far:battery-empty` | `bs:battery`（） |
| battery-exclamation | `fas:battery-exclamation` / `far:battery-exclamation` | `bs:battery`（） |
| battery-full | `fas:battery-full` / `far:battery-full` | `bs:battery-full`（满电） |
| battery-half | `fas:battery-half` / `far:battery-half` | `bs:battery-half`（） |
| battery-quarter | `fas:battery-quarter` / `far:battery-quarter` | `bs:battery-quarter`（） |
| battery-three-quarters | `fas:battery-three-quarters` / `far:battery-three-quarters` | `bs:battery-three-quarters`（） |
| beaker | `fas:beaker` / `far:beaker` | `bs:flask`（） |
| bed-pulse | `fas:bed-pulse` / `far:bed-pulse` | `bs:heart-pulse`（） |
| beer-mug-empty | `fas:beer-mug-empty` / `far:beer-mug-empty` | `bs:cup`（） |
| bell | `fas:bell` / `far:bell` | `bs:bell`（铃铛） |
| bell-exclamation | `fas:bell-exclamation` / `far:bell-exclamation` | `bs:bell`（铃铛） |
| bell-regular | `fas:bell-regular` / `far:bell-regular` | `bs:bell`（铃铛） |
| bell-ring | `fas:bell-ring` / `far:bell-ring` | `bs:bell-fill`（铃铛实心） |
| bell-school | `fas:bell-school` / `far:bell-school` | `bs:bell`（铃铛） |
| bell-slash | `fas:bell-slash` / `far:bell-slash` | `bs:bell-slash`（） |
| bell-slash-regular | `fas:bell-slash-regular` / `far:bell-slash-regular` | `bs:bell-slash`（） |
| bezier-curve | `fas:bezier-curve` / `far:bezier-curve` | `bs:bezier2`（） |
| bicycle | `fas:bicycle` / `far:bicycle` | `bs:bicycle`（） |
| binoculars | `fas:binoculars` / `far:binoculars` | `bs:binoculars`（） |
| birthday-cake | `fas:birthday-cake` / `far:birthday-cake` | `bs:cake`（） |
| bitcoin-sign | `fas:bitcoin-sign` / `far:bitcoin-sign` | `bs:currency-bitcoin`（） |
| bluetooth | `fas:bluetooth` / `far:bluetooth` | `bs:bluetooth`（） |
| bolt | `fas:bolt` / `far:bolt` | `bs:lightning-charge`（闪电） |
| bolt-lightning | `fas:bolt-lightning` / `far:bolt-lightning` | `bs:lightning-charge`（闪电） |
| bolt-regular-regular | `fas:bolt-regular-regular` / `far:bolt-regular-regular` | `bs:lightning-charge`（闪电） |
| bomb | `fas:bomb` / `far:bomb` | `bs:bomb`（） |
| bomb-regular | `fas:bomb-regular` / `far:bomb-regular` | `bs:bomb`（） |
| bone | `fas:bone` / `far:bone` | `bs:bone`（） |
| bone-regular | `fas:bone-regular` / `far:bone-regular` | `bs:bone`（） |
| book | `fas:book` / `far:book` | `bs:journal-text`（日志） |
| book-open | `fas:book-open` / `far:book-open` | `bs:journal-text`（日志） |
| book-reader | `fas:book-reader` / `far:book-reader` | `bs:book`（） |
| bookmark | `fas:bookmark` / `far:bookmark` | `bs:bookmark-star`（书签星标） |
| bookmark-regular | `fas:bookmark-regular` / `far:bookmark-regular` | `bs:bookmark-star`（书签星标） |
| bookmark-regular-regular | `fas:bookmark-regular-regular` / `far:bookmark-regular-regular` | `bs:bookmark-star`（书签星标） |
| books | `fas:books` / `far:books` | `bs:journal-text`（日志） |
| bottle-droplet | `fas:bottle-droplet` / `far:bottle-droplet` | `bs:droplet`（水滴） |
| bottle-regular | `fas:bottle-regular` / `far:bottle-regular` | `bs:droplet`（水滴） |
| bottle-water | `fas:bottle-water` / `far:bottle-water` | `bs:droplet`（水滴） |
| bowl-food | `fas:bowl-food` / `far:bowl-food` | `bs:egg`（） |
| bowl-regular | `fas:bowl-regular` / `far:bowl-regular` | `bs:egg`（） |
| bowl-rice | `fas:bowl-rice` / `far:bowl-rice` | `bs:egg`（） |
| bowling-ball | `fas:bowling-ball` / `far:bowling-ball` | `bs:trophy`（奖杯） |
| box | `fas:box` / `far:box` | `bs:box`（盒子） |
| box-archive | `fas:box-archive` / `far:box-archive` | `bs:archive`（归档箱） |
| box-open | `fas:box-open` / `far:box-open` | `bs:box-seam`（包裹） |
| box-open-regular | `fas:box-open-regular` / `far:box-open-regular` | `bs:box-seam`（包裹） |
| box-regular | `fas:box-regular` / `far:box-regular` | `bs:box`（盒子） |
| boxes-stacked | `fas:boxes-stacked` / `far:boxes-stacked` | `bs:boxes`（） |
| brain | `fas:brain` / `far:brain` | `bs:brain`（） |
| bridge | `fas:bridge` / `far:bridge` | `bs:building`（大楼） |
| bridge-circle-exclamation | `fas:bridge-circle-exclamation` / `far:bridge-circle-exclamation` | `bs:building`（大楼） |
| bridge-circle-xmark | `fas:bridge-circle-xmark` / `far:bridge-circle-xmark` | `bs:building`（大楼） |
| bridge-lock | `fas:bridge-lock` / `far:bridge-lock` | `bs:building`（大楼） |
| bridge-regular-regular | `fas:bridge-regular-regular` / `far:bridge-regular-regular` | `bs:building`（大楼） |
| bridge-water | `fas:bridge-water` / `far:bridge-water` | `bs:building`（大楼） |
| briefcase | `fas:briefcase` / `far:briefcase` | `bs:briefcase`（） |
| briefcase-medical | `fas:briefcase-medical` / `far:briefcase-medical` | `bs:briefcase`（） |
| briefcase-medical-regular | `fas:briefcase-medical-regular` / `far:briefcase-medical-regular` | `bs:briefcase`（） |
| broadcast-regular | `fas:broadcast-regular` / `far:broadcast-regular` | `bs:broadcast`（） |
| broadcast-tower | `fas:broadcast-tower` / `far:broadcast-tower` | `bs:broadcast`（） |
| broom | `fas:broom` / `far:broom` | `bs:broom`（） |
| broom-ball | `fas:broom-ball` / `far:broom-ball` | `bs:broom`（） |
| broom-regular | `fas:broom-regular` / `far:broom-regular` | `bs:broom`（） |
| bug | `fas:bug` / `far:bug` | `bs:bug`（） |
| building | `fas:building` / `far:building` | `bs:building`（大楼） |
| building-columns | `fas:building-columns` / `far:building-columns` | `bs:bank`（银行） |
| building-flag | `fas:building-flag` / `far:building-flag` | `bs:building`（大楼） |
| building-lock | `fas:building-lock` / `far:building-lock` | `bs:building`（大楼） |
| building-shield | `fas:building-shield` / `far:building-shield` | `bs:building`（大楼） |
| building-user | `fas:building-user` / `far:building-user` | `bs:building`（大楼） |
| bullseye-regular | `fas:bullseye-regular` / `far:bullseye-regular` | `bs:bullseye`（靶心） |
| burger | `fas:burger` / `far:burger` | `bs:egg`（） |
| burn | `fas:burn` / `far:burn` | `bs:fire`（火焰） |
| burst | `fas:burst` / `far:burst` | `bs:boom`（） |
| burst-regular | `fas:burst-regular` / `far:burst-regular` | `bs:boom`（） |
| bus | `fas:bus` / `far:bus` | `bs:bus-front`（） |
| cactus | `fas:cactus` / `far:cactus` | `bs:tree`（） |
| cactus-regular | `fas:cactus-regular` / `far:cactus-regular` | `bs:tree`（） |
| cake-candles | `fas:cake-candles` / `far:cake-candles` | `bs:cake`（） |
| calculator | `fas:calculator` / `far:calculator` | `bs:calculator`（计算器） |
| calendar | `fas:calendar` / `far:calendar` | `bs:calendar`（日历） |
| calendar-check | `fas:calendar-check` / `far:calendar-check` | `bs:calendar-check`（日历对勾） |
| calendar-day | `fas:calendar-day` / `far:calendar-day` | `bs:calendar-date`（日历日期） |
| calendar-days | `fas:calendar-days` / `far:calendar-days` | `bs:calendar-date`（日历日期） |
| calendar-minus | `fas:calendar-minus` / `far:calendar-minus` | `bs:calendar-minus`（） |
| calendar-minus-regular | `fas:calendar-minus-regular` / `far:calendar-minus-regular` | `bs:calendar-minus`（） |
| calendar-star | `fas:calendar-star` / `far:calendar-star` | `bs:calendar-star`（） |
| calendar-week | `fas:calendar-week` / `far:calendar-week` | `bs:calendar-week`（） |
| calendar-xmark | `fas:calendar-xmark` / `far:calendar-xmark` | `bs:calendar-x`（） |
| camera | `fas:camera` / `far:camera` | `bs:camera`（相机） |
| campfire | `fas:campfire` / `far:campfire` | `bs:fire`（火焰） |
| campground | `fas:campground` / `far:campground` | `bs:moon-stars`（星月） |
| candy | `fas:candy` / `far:candy` | `bs:egg`（） |
| cane | `fas:cane` / `far:cane` | `bs:person-walking`（） |
| capsules | `fas:capsules` / `far:capsules` | `bs:capsule`（） |
| car | `fas:car` / `far:car` | `bs:car-front`（） |
| car-battery | `fas:car-battery` / `far:car-battery` | `bs:battery-charging`（） |
| car-rear | `fas:car-rear` / `far:car-rear` | `bs:car-front`（） |
| car-side | `fas:car-side` / `far:car-side` | `bs:car-front`（） |
| caret-down | `fas:caret-down` / `far:caret-down` | `bs:chevron-down`（下尖括号） |
| caret-left | `fas:caret-left` / `far:caret-left` | `bs:chevron-left`（左尖括号） |
| caret-right | `fas:caret-right` / `far:caret-right` | `bs:chevron-right`（右尖括号） |
| caret-up | `fas:caret-up` / `far:caret-up` | `bs:chevron-up`（上尖括号） |
| carrot | `fas:carrot` / `far:carrot` | `bs:carrot`（） |
| cart-arrow-down | `fas:cart-arrow-down` / `far:cart-arrow-down` | `bs:cart`（） |
| cart-flatbed | `fas:cart-flatbed` / `far:cart-flatbed` | `bs:cart`（） |
| cart-plus | `fas:cart-plus` / `far:cart-plus` | `bs:cart-plus`（） |
| cart-shopping | `fas:cart-shopping` / `far:cart-shopping` | `bs:cart`（） |
| castle | `fas:castle` / `far:castle` | `bs:building`（大楼） |
| cat | `fas:cat` / `far:cat` | `bs:cat`（） |
| cedi-sign | `fas:cedi-sign` / `far:cedi-sign` | `bs:currency-dollar`（美元） |
| chain | `fas:chain` / `far:chain` | `bs:link-45deg`（链接） |
| chalkboard | `fas:chalkboard` / `far:chalkboard` | `bs:display`（显示器） |
| chalkboard-user | `fas:chalkboard-user` / `far:chalkboard-user` | `bs:person-badge`（人员徽章） |
| champagne-glasses | `fas:champagne-glasses` / `far:champagne-glasses` | `bs:cup`（） |
| charging-station | `fas:charging-station` / `far:charging-station` | `bs:fuel-pump`（） |
| chart-area | `fas:chart-area` / `far:chart-area` | `bs:graph-up-arrow`（上升箭头图） |
| chart-bar | `fas:chart-bar` / `far:chart-bar` | `bs:bar-chart`（柱状图） |
| chart-column | `fas:chart-column` / `far:chart-column` | `bs:bar-chart`（柱状图） |
| chart-gantt | `fas:chart-gantt` / `far:chart-gantt` | `bs:bar-chart-steps`（阶梯图） |
| chart-line | `fas:chart-line` / `far:chart-line` | `bs:graph-up`（上升折线） |
| chart-pie | `fas:chart-pie` / `far:chart-pie` | `bs:pie-chart`（饼图） |
| chart-pie-slice | `fas:chart-pie-slice` / `far:chart-pie-slice` | `bs:pie-chart`（饼图） |
| chart-scatter | `fas:chart-scatter` / `far:chart-scatter` | `bs:graph-up`（上升折线） |
| chart-simple | `fas:chart-simple` / `far:chart-simple` | `bs:graph-up`（上升折线） |
| check | `fas:check` / `far:check` | `bs:check`（对勾） |
| check-double | `fas:check-double` / `far:check-double` | `bs:check2-all`（全部完成） |
| check-regular | `fas:check-regular` / `far:check-regular` | `bs:check`（对勾） |
| cheese | `fas:cheese` / `far:cheese` | `bs:egg`（） |
| chess | `fas:chess` / `far:chess` | `bs:chess-knight`（） |
| chess-bishop | `fas:chess-bishop` / `far:chess-bishop` | `bs:chess-knight`（） |
| chess-board | `fas:chess-board` / `far:chess-board` | `bs:grid`（网格） |
| chess-king | `fas:chess-king` / `far:chess-king` | `bs:chess-knight`（） |
| chess-knight | `fas:chess-knight` / `far:chess-knight` | `bs:chess-knight`（） |
| chess-knight-regular | `fas:chess-knight-regular` / `far:chess-knight-regular` | `bs:chess-knight`（） |
| chess-pawn | `fas:chess-pawn` / `far:chess-pawn` | `bs:chess-knight`（） |
| chess-queen | `fas:chess-queen` / `far:chess-queen` | `bs:chess-knight`（） |
| chess-rook | `fas:chess-rook` / `far:chess-rook` | `bs:chess-knight`（） |
| chevron-down | `fas:chevron-down` / `far:chevron-down` | `bs:chevron-down`（下尖括号） |
| chevron-left | `fas:chevron-left` / `far:chevron-left` | `bs:chevron-left`（左尖括号） |
| chevron-right | `fas:chevron-right` / `far:chevron-right` | `bs:chevron-right`（右尖括号） |
| chevron-up | `fas:chevron-up` / `far:chevron-up` | `bs:chevron-up`（上尖括号） |
| child | `fas:child` / `far:child` | `bs:child`（） |
| child-regular | `fas:child-regular` / `far:child-regular` | `bs:child`（） |
| children | `fas:children` / `far:children` | `bs:people`（多人） |
| church | `fas:church` / `far:church` | `bs:building`（大楼） |
| cigarette | `fas:cigarette` / `far:cigarette` | `bs:cloud-fog2`（） |
| circle | `fas:circle` / `far:circle` | `bs:circle`（） |
| circle-arrow-down | `fas:circle-arrow-down` / `far:circle-arrow-down` | `bs:arrow-down-circle`（下箭头圆） |
| circle-arrow-left | `fas:circle-arrow-left` / `far:circle-arrow-left` | `bs:arrow-left-circle`（左箭头圆） |
| circle-arrow-right | `fas:circle-arrow-right` / `far:circle-arrow-right` | `bs:arrow-right-circle`（右箭头圆） |
| circle-arrow-up | `fas:circle-arrow-up` / `far:circle-arrow-up` | `bs:arrow-up-circle`（上箭头圆） |
| circle-check | `fas:circle-check` / `far:circle-check` | `bs:check-circle`（对勾圆圈） |
| circle-check-regular | `fas:circle-check-regular` / `far:circle-check-regular` | `bs:check-circle`（对勾圆圈） |
| circle-chevron-down | `fas:circle-chevron-down` / `far:circle-chevron-down` | `bs:arrow-down-circle`（下箭头圆） |
| circle-chevron-left | `fas:circle-chevron-left` / `far:circle-chevron-left` | `bs:arrow-left-circle`（左箭头圆） |
| circle-chevron-right | `fas:circle-chevron-right` / `far:circle-chevron-right` | `bs:arrow-right-circle`（右箭头圆） |
| circle-chevron-up | `fas:circle-chevron-up` / `far:circle-chevron-up` | `bs:arrow-up-circle`（上箭头圆） |
| circle-dot | `fas:circle-dot` / `far:circle-dot` | `bs:record-circle`（） |
| circle-down | `fas:circle-down` / `far:circle-down` | `bs:arrow-down-circle`（下箭头圆） |
| circle-exclamation | `fas:circle-exclamation` / `far:circle-exclamation` | `bs:exclamation-circle`（感叹圆圈） |
| circle-half | `fas:circle-half` / `far:circle-half` | `bs:circle-half`（） |
| circle-half-stroke | `fas:circle-half-stroke` / `far:circle-half-stroke` | `bs:circle-half`（） |
| circle-info | `fas:circle-info` / `far:circle-info` | `bs:info-circle`（信息圆圈） |
| circle-left | `fas:circle-left` / `far:circle-left` | `bs:arrow-left-circle`（左箭头圆） |
| circle-minus | `fas:circle-minus` / `far:circle-minus` | `bs:dash-circle`（） |
| circle-notch | `fas:circle-notch` / `far:circle-notch` | `bs:circle`（） |
| circle-pause | `fas:circle-pause` / `far:circle-pause` | `bs:pause-circle`（暂停） |
| circle-play | `fas:circle-play` / `far:circle-play` | `bs:play-circle`（播放） |
| circle-plus | `fas:circle-plus` / `far:circle-plus` | `bs:plus-circle`（） |
| circle-quarter | `fas:circle-quarter` / `far:circle-quarter` | `bs:circle`（） |
| circle-question | `fas:circle-question` / `far:circle-question` | `bs:question-circle`（问号圆圈） |
| circle-record | `fas:circle-record` / `far:circle-record` | `bs:record-circle`（） |
| circle-regular | `fas:circle-regular` / `far:circle-regular` | `bs:circle`（） |
| circle-regular-regular | `fas:circle-regular-regular` / `far:circle-regular-regular` | `bs:circle`（） |
| circle-right | `fas:circle-right` / `far:circle-right` | `bs:arrow-right-circle`（右箭头圆） |
| circle-small | `fas:circle-small` / `far:circle-small` | `bs:circle`（） |
| circle-stop | `fas:circle-stop` / `far:circle-stop` | `bs:stop-circle`（停止） |
| circle-three-quarters | `fas:circle-three-quarters` / `far:circle-three-quarters` | `bs:circle`（） |
| circle-up | `fas:circle-up` / `far:circle-up` | `bs:arrow-up-circle`（上箭头圆） |
| circle-user | `fas:circle-user` / `far:circle-user` | `bs:person-circle`（人员头像） |
| circle-xmark | `fas:circle-xmark` / `far:circle-xmark` | `bs:x-circle`（叉号圆圈） |
| city | `fas:city` / `far:city` | `bs:building`（大楼） |
| clapperboard | `fas:clapperboard` / `far:clapperboard` | `bs:film`（） |
| clapperboard-play | `fas:clapperboard-play` / `far:clapperboard-play` | `bs:film`（） |
| clipboard | `fas:clipboard` / `far:clipboard` | `bs:clipboard-check`（剪贴板对勾） |
| clipboard-pen | `fas:clipboard-pen` / `far:clipboard-pen` | `bs:clipboard-data`（剪贴板数据） |
| clipboard-question | `fas:clipboard-question` / `far:clipboard-question` | `bs:clipboard-check`（剪贴板对勾） |
| clipboard-user | `fas:clipboard-user` / `far:clipboard-user` | `bs:person-badge`（人员徽章） |
| clock | `fas:clock` / `far:clock` | `bs:clock`（时钟） |
| clock-rotate-left | `fas:clock-rotate-left` / `far:clock-rotate-left` | `bs:clock-history`（历史时钟） |
| cloud | `fas:cloud` / `far:cloud` | `bs:cloud`（云朵） |
| cloud-arrow-down | `fas:cloud-arrow-down` / `far:cloud-arrow-down` | `bs:cloud-arrow-down`（云端下载） |
| cloud-arrow-up | `fas:cloud-arrow-up` / `far:cloud-arrow-up` | `bs:cloud-arrow-up`（云端上传） |
| cloud-bolt | `fas:cloud-bolt` / `far:cloud-bolt` | `bs:cloud-lightning-rain`（） |
| cloud-fog | `fas:cloud-fog` / `far:cloud-fog` | `bs:cloud-fog2`（） |
| cloud-hail | `fas:cloud-hail` / `far:cloud-hail` | `bs:cloud-hail`（） |
| cloud-moon-rain | `fas:cloud-moon-rain` / `far:cloud-moon-rain` | `bs:cloud-moon`（） |
| cloud-rain | `fas:cloud-rain` / `far:cloud-rain` | `bs:cloud-rain`（） |
| cloud-regular-regular | `fas:cloud-regular-regular` / `far:cloud-regular-regular` | `bs:cloud`（云朵） |
| cloud-showers | `fas:cloud-showers` / `far:cloud-showers` | `bs:cloud-rain`（） |
| cloud-showers-heavy | `fas:cloud-showers-heavy` / `far:cloud-showers-heavy` | `bs:cloud-rain-heavy`（） |
| cloud-showers-heavy-regular | `fas:cloud-showers-heavy-regular` / `far:cloud-showers-heavy-regular` | `bs:cloud-rain-heavy`（） |
| cloud-snow | `fas:cloud-snow` / `far:cloud-snow` | `bs:cloud-snow`（） |
| cny-sign | `fas:cny-sign` / `far:cny-sign` | `bs:currency-yen`（日元） |
| cocktail | `fas:cocktail` / `far:cocktail` | `bs:cup`（） |
| code | `fas:code` / `far:code` | `bs:code-slash`（） |
| code-regular | `fas:code-regular` / `far:code-regular` | `bs:code`（） |
| coffee | `fas:coffee` / `far:coffee` | `bs:cup`（） |
| cog | `fas:cog` / `far:cog` | `bs:gear`（齿轮） |
| coins | `fas:coins` / `far:coins` | `bs:coins`（） |
| colon-sign | `fas:colon-sign` / `far:colon-sign` | `bs:currency-dollar`（美元） |
| columns | `fas:columns` / `far:columns` | `bs:columns`（） |
| comet | `fas:comet` / `far:comet` | `bs:star`（星形） |
| compass | `fas:compass` / `far:compass` | `bs:compass`（罗盘） |
| compass-drafting | `fas:compass-drafting` / `far:compass-drafting` | `bs:compass`（罗盘） |
| compass-drafting-regular | `fas:compass-drafting-regular` / `far:compass-drafting-regular` | `bs:compass`（罗盘） |
| compass-regular | `fas:compass-regular` / `far:compass-regular` | `bs:compass`（罗盘） |
| compress | `fas:compress` / `far:compress` | `bs:arrows-collapse`（向内收缩） |
| computer | `fas:computer` / `far:computer` | `bs:display`（显示器） |
| computer-mouse | `fas:computer-mouse` / `far:computer-mouse` | `bs:mouse`（） |
| cookie | `fas:cookie` / `far:cookie` | `bs:egg`（） |
| cookie-bite | `fas:cookie-bite` / `far:cookie-bite` | `bs:egg`（） |
| copy | `fas:copy` / `far:copy` | `bs:files`（多文件） |
| cow | `fas:cow` / `far:cow` | `bs:bug`（） |
| credit-card | `fas:credit-card` / `far:credit-card` | `bs:credit-card`（信用卡） |
| cross | `fas:cross` / `far:cross` | `bs:plus`（加号） |
| crosshairs | `fas:crosshairs` / `far:crosshairs` | `bs:bullseye`（靶心） |
| crow | `fas:crow` / `far:crow` | `bs:bird`（） |
| crown | `fas:crown` / `far:crown` | `bs:crown`（） |
| crown-regular | `fas:crown-regular` / `far:crown-regular` | `bs:crown`（） |
| crown-regular-regular | `fas:crown-regular-regular` / `far:crown-regular-regular` | `bs:crown`（） |
| crutch | `fas:crutch` / `far:crutch` | `bs:bandage`（） |
| cruzeiro-sign | `fas:cruzeiro-sign` / `far:cruzeiro-sign` | `bs:currency-dollar`（美元） |
| cube | `fas:cube` / `far:cube` | `bs:box`（盒子） |
| cubes | `fas:cubes` / `far:cubes` | `bs:boxes`（） |
| cup-regular | `fas:cup-regular` / `far:cup-regular` | `bs:cup`（） |
| database | `fas:database` / `far:database` | `bs:database`（） |
| database-arrow-down | `fas:database-arrow-down` / `far:database-arrow-down` | `bs:database`（） |
| database-arrow-up | `fas:database-arrow-up` / `far:database-arrow-up` | `bs:database`（） |
| database-check | `fas:database-check` / `far:database-check` | `bs:database`（） |
| database-lock | `fas:database-lock` / `far:database-lock` | `bs:database`（） |
| database-plus | `fas:database-plus` / `far:database-plus` | `bs:database-plus`（） |
| database-regular-regular | `fas:database-regular-regular` / `far:database-regular-regular` | `bs:database`（） |
| diagram-project | `fas:diagram-project` / `far:diagram-project` | `bs:diagram-3`（） |
| diagram-successor | `fas:diagram-successor` / `far:diagram-successor` | `bs:diagram-3`（） |
| dice | `fas:dice` / `far:dice` | `bs:dice`（） |
| dice-d20 | `fas:dice-d20` / `far:dice-d20` | `bs:dice-6`（） |
| dice-d6 | `fas:dice-d6` / `far:dice-d6` | `bs:dice-6`（） |
| dice-five | `fas:dice-five` / `far:dice-five` | `bs:dice-5`（） |
| dice-four | `fas:dice-four` / `far:dice-four` | `bs:dice-4`（） |
| dice-one | `fas:dice-one` / `far:dice-one` | `bs:dice-1`（） |
| dice-regular | `fas:dice-regular` / `far:dice-regular` | `bs:dice`（） |
| dice-six | `fas:dice-six` / `far:dice-six` | `bs:dice-6`（） |
| dice-three | `fas:dice-three` / `far:dice-three` | `bs:dice-3`（） |
| dice-two | `fas:dice-two` / `far:dice-two` | `bs:dice-2`（） |
| divide | `fas:divide` / `far:divide` | `bs:dash`（横杠） |
| dna | `fas:dna` / `far:dna` | `bs:dna`（） |
| dna-regular | `fas:dna-regular` / `far:dna-regular` | `bs:dna`（） |
| dog | `fas:dog` / `far:dog` | `bs:dog`（） |
| dollar-sign | `fas:dollar-sign` / `far:dollar-sign` | `bs:currency-dollar`（美元） |
| dong-sign | `fas:dong-sign` / `far:dong-sign` | `bs:currency-dollar`（美元） |
| dot-circle | `fas:dot-circle` / `far:dot-circle` | `bs:record-circle`（） |
| dove | `fas:dove` / `far:dove` | `bs:bird`（） |
| down-up | `fas:down-up` / `far:down-up` | `bs:arrow-up-down`（） |
| download | `fas:download` / `far:download` | `bs:download`（下载） |
| dragon | `fas:dragon` / `far:dragon` | `bs:dragon`（） |
| draw-polygon | `fas:draw-polygon` / `far:draw-polygon` | `bs:hexagon`（） |
| droplet | `fas:droplet` / `far:droplet` | `bs:droplet`（水滴） |
| drum | `fas:drum` / `far:drum` | `bs:music-note`（音符） |
| drum-regular | `fas:drum-regular` / `far:drum-regular` | `bs:music-note`（音符） |
| drumstick-bite | `fas:drumstick-bite` / `far:drumstick-bite` | `bs:egg`（） |
| dumbbell | `fas:dumbbell` / `far:dumbbell` | `bs:activity`（脉搏） |
| ear-deaf | `fas:ear-deaf` / `far:ear-deaf` | `bs:headset`（耳麦） |
| ear-listen | `fas:ear-listen` / `far:ear-listen` | `bs:headset`（耳麦） |
| earth-africa | `fas:earth-africa` / `far:earth-africa` | `bs:globe-europe-africa`（） |
| earth-americas | `fas:earth-americas` / `far:earth-americas` | `bs:globe-americas`（美洲地球） |
| earth-asia | `fas:earth-asia` / `far:earth-asia` | `bs:globe-asia-australia`（） |
| earth-europe | `fas:earth-europe` / `far:earth-europe` | `bs:globe-europe-africa`（） |
| earth-oceania | `fas:earth-oceania` / `far:earth-oceania` | `bs:globe-asia-australia`（） |
| eject | `fas:eject` / `far:eject` | `bs:skip-end`（） |
| eject-regular | `fas:eject-regular` / `far:eject-regular` | `bs:skip-end`（） |
| ellipsis | `fas:ellipsis` / `far:ellipsis` | `bs:three-dots`（） |
| ellipsis-vertical | `fas:ellipsis-vertical` / `far:ellipsis-vertical` | `bs:three-dots-vertical`（） |
| envelope | `fas:envelope` / `far:envelope` | `bs:envelope`（邮件） |
| envelope-circle-arrow-right | `fas:envelope-circle-arrow-right` / `far:envelope-circle-arrow-right` | `bs:envelope`（邮件） |
| envelope-circle-check | `fas:envelope-circle-check` / `far:envelope-circle-check` | `bs:envelope-check`（） |
| envelope-open | `fas:envelope-open` / `far:envelope-open` | `bs:envelope-open`（） |
| envelope-paper-plane | `fas:envelope-paper-plane` / `far:envelope-paper-plane` | `bs:send`（发送） |
| equals | `fas:equals` / `far:equals` | `bs:equal`（） |
| eraser | `fas:eraser` / `far:eraser` | `bs:eraser`（） |
| euro-sign | `fas:euro-sign` / `far:euro-sign` | `bs:currency-euro`（欧元） |
| evergreen | `fas:evergreen` / `far:evergreen` | `bs:tree`（） |
| exclamation | `fas:exclamation` / `far:exclamation` | `bs:exclamation-lg`（） |
| exclamation-regular | `fas:exclamation-regular` / `far:exclamation-regular` | `bs:exclamation-lg`（） |
| expand | `fas:expand` / `far:expand` | `bs:arrows-expand`（向外展开） |
| explosion | `fas:explosion` / `far:explosion` | `bs:boom`（） |
| explosion-regular | `fas:explosion-regular` / `far:explosion-regular` | `bs:boom`（） |
| external-link | `fas:external-link` / `far:external-link` | `bs:box-arrow-up-right`（外链） |
| eye | `fas:eye` / `far:eye` | `bs:eye`（眼睛） |
| eye-slash | `fas:eye-slash` / `far:eye-slash` | `bs:eye-slash`（闭眼） |
| eyedropper | `fas:eyedropper` / `far:eyedropper` | `bs:eyedropper`（） |
| face-frown | `fas:face-frown` / `far:face-frown` | `bs:emoji-frown`（） |
| face-grin | `fas:face-grin` / `far:face-grin` | `bs:emoji-laughing`（） |
| face-meh | `fas:face-meh` / `far:face-meh` | `bs:emoji-neutral`（） |
| face-smile | `fas:face-smile` / `far:face-smile` | `bs:emoji-smile`（） |
| factory | `fas:factory` / `far:factory` | `bs:building`（大楼） |
| fan | `fas:fan` / `far:fan` | `bs:fan`（） |
| fax | `fas:fax` / `far:fax` | `bs:printer`（打印机） |
| feather | `fas:feather` / `far:feather` | `bs:feather`（） |
| feather-pointed | `fas:feather-pointed` / `far:feather-pointed` | `bs:feather`（） |
| file | `fas:file` / `far:file` | `bs:file-text`（文档） |
| file-archive | `fas:file-archive` / `far:file-archive` | `bs:file-zip`（） |
| file-arrow-down | `fas:file-arrow-down` / `far:file-arrow-down` | `bs:file-arrow-down`（） |
| file-arrow-up | `fas:file-arrow-up` / `far:file-arrow-up` | `bs:file-arrow-up`（） |
| file-audio | `fas:file-audio` / `far:file-audio` | `bs:file-music`（） |
| file-circle-plus | `fas:file-circle-plus` / `far:file-circle-plus` | `bs:file-plus`（） |
| file-code | `fas:file-code` / `far:file-code` | `bs:file-code`（） |
| file-contract | `fas:file-contract` / `far:file-contract` | `bs:file-text`（文档） |
| file-csv | `fas:file-csv` / `far:file-csv` | `bs:file-spreadsheet`（表格文档） |
| file-excel | `fas:file-excel` / `far:file-excel` | `bs:file-spreadsheet`（表格文档） |
| file-export | `fas:file-export` / `far:file-export` | `bs:box-arrow-up-right`（外链） |
| file-image | `fas:file-image` / `far:file-image` | `bs:file-image`（） |
| file-import | `fas:file-import` / `far:file-import` | `bs:box-arrow-in-right`（） |
| file-invoice | `fas:file-invoice` / `far:file-invoice` | `bs:file-text`（文档） |
| file-invoice-dollar | `fas:file-invoice-dollar` / `far:file-invoice-dollar` | `bs:file-text`（文档） |
| file-lines | `fas:file-lines` / `far:file-lines` | `bs:file-text`（文档） |
| file-lock | `fas:file-lock` / `far:file-lock` | `bs:file-text`（文档） |
| file-magnifying-glass | `fas:file-magnifying-glass` / `far:file-magnifying-glass` | `bs:file-text`（文档） |
| file-magnifying-glass-regular | `fas:file-magnifying-glass-regular` / `far:file-magnifying-glass-regular` | `bs:file-text`（文档） |
| file-pen | `fas:file-pen` / `far:file-pen` | `bs:file-edit`（） |
| file-powerpoint | `fas:file-powerpoint` / `far:file-powerpoint` | `bs:file-ppt`（） |
| file-shield | `fas:file-shield` / `far:file-shield` | `bs:file-text`（文档） |
| file-signature | `fas:file-signature` / `far:file-signature` | `bs:file-text`（文档） |
| file-video | `fas:file-video` / `far:file-video` | `bs:file-play`（） |
| file-zipper | `fas:file-zipper` / `far:file-zipper` | `bs:file-zip`（） |
| fill | `fas:fill` / `far:fill` | `bs:droplet-fill`（） |
| film | `fas:film` / `far:film` | `bs:film`（） |
| filter | `fas:filter` / `far:filter` | `bs:funnel`（漏斗） |
| fingerprint | `fas:fingerprint` / `far:fingerprint` | `bs:fingerprint`（指纹） |
| fire | `fas:fire` / `far:fire` | `bs:fire`（火焰） |
| fire-extinguisher | `fas:fire-extinguisher` / `far:fire-extinguisher` | `bs:fire`（火焰） |
| fire-flame-curved | `fas:fire-flame-curved` / `far:fire-flame-curved` | `bs:fire`（火焰） |
| fire-flame-simple | `fas:fire-flame-simple` / `far:fire-flame-simple` | `bs:fire`（火焰） |
| fire-regular | `fas:fire-regular` / `far:fire-regular` | `bs:fire`（火焰） |
| fish | `fas:fish` / `far:fish` | `bs:fish`（） |
| flag | `fas:flag` / `far:flag` | `bs:flag`（旗帜） |
| flag-checkered | `fas:flag-checkered` / `far:flag-checkered` | `bs:flag`（旗帜） |
| flag-pennant | `fas:flag-pennant` / `far:flag-pennant` | `bs:flag`（旗帜） |
| flag-regular | `fas:flag-regular` / `far:flag-regular` | `bs:flag`（旗帜） |
| flag-regular-regular-regular | `fas:flag-regular-regular-regular` / `far:flag-regular-regular-regular` | `bs:flag`（旗帜） |
| flag-usa | `fas:flag-usa` / `far:flag-usa` | `bs:flag`（旗帜） |
| flame | `fas:flame` / `far:flame` | `bs:fire`（火焰） |
| flashlight | `fas:flashlight` / `far:flashlight` | `bs:lightbulb-fill`（灯泡实心） |
| flask | `fas:flask` / `far:flask` | `bs:flask`（） |
| flask-vial | `fas:flask-vial` / `far:flask-vial` | `bs:flask`（） |
| flip-horizontal | `fas:flip-horizontal` / `far:flip-horizontal` | `bs:arrow-left-right`（） |
| flip-vertical | `fas:flip-vertical` / `far:flip-vertical` | `bs:arrow-up-down`（） |
| floppy-disk | `fas:floppy-disk` / `far:floppy-disk` | `bs:save`（） |
| florin-sign | `fas:florin-sign` / `far:florin-sign` | `bs:currency-dollar`（美元） |
| flower | `fas:flower` / `far:flower` | `bs:flower1`（花朵） |
| flower-daffodil | `fas:flower-daffodil` / `far:flower-daffodil` | `bs:flower1`（花朵） |
| flower-regular | `fas:flower-regular` / `far:flower-regular` | `bs:flower1`（花朵） |
| flower-tulip | `fas:flower-tulip` / `far:flower-tulip` | `bs:flower1`（花朵） |
| folder | `fas:folder` / `far:folder` | `bs:folder`（文件夹） |
| folder-closed | `fas:folder-closed` / `far:folder-closed` | `bs:folder`（文件夹） |
| folder-closed-regular | `fas:folder-closed-regular` / `far:folder-closed-regular` | `bs:folder`（文件夹） |
| folder-film | `fas:folder-film` / `far:folder-film` | `bs:folder`（文件夹） |
| folder-gear | `fas:folder-gear` / `far:folder-gear` | `bs:folder`（文件夹） |
| folder-heart | `fas:folder-heart` / `far:folder-heart` | `bs:folder`（文件夹） |
| folder-minus | `fas:folder-minus` / `far:folder-minus` | `bs:folder-minus`（） |
| folder-music | `fas:folder-music` / `far:folder-music` | `bs:folder`（文件夹） |
| folder-open | `fas:folder-open` / `far:folder-open` | `bs:folder2-open`（打开文件夹） |
| folder-plus | `fas:folder-plus` / `far:folder-plus` | `bs:folder-plus`（） |
| folder-plus-regular | `fas:folder-plus-regular` / `far:folder-plus-regular` | `bs:folder-plus`（） |
| folder-tree | `fas:folder-tree` / `far:folder-tree` | `bs:folder`（文件夹） |
| folder-tree-regular | `fas:folder-tree-regular` / `far:folder-tree-regular` | `bs:folder`（文件夹） |
| folder-video | `fas:folder-video` / `far:folder-video` | `bs:folder`（文件夹） |
| football | `fas:football` / `far:football` | `bs:trophy`（奖杯） |
| fork-knife | `fas:fork-knife` / `far:fork-knife` | `bs:cup`（） |
| fort | `fas:fort` / `far:fort` | `bs:building`（大楼） |
| forward | `fas:forward` / `far:forward` | `bs:forward`（快进） |
| forward-regular | `fas:forward-regular` / `far:forward-regular` | `bs:forward`（快进） |
| forward-regular-regular | `fas:forward-regular-regular` / `far:forward-regular-regular` | `bs:forward`（快进） |
| forward-step | `fas:forward-step` / `far:forward-step` | `bs:skip-end`（） |
| franc-sign | `fas:franc-sign` / `far:franc-sign` | `bs:currency-dollar`（美元） |
| futbol | `fas:futbol` / `far:futbol` | `bs:trophy`（奖杯） |
| futbol-regular | `fas:futbol-regular` / `far:futbol-regular` | `bs:trophy`（奖杯） |
| gamepad | `fas:gamepad` / `far:gamepad` | `bs:controller`（） |
| gas-pump | `fas:gas-pump` / `far:gas-pump` | `bs:fuel-pump`（） |
| gauge | `fas:gauge` / `far:gauge` | `bs:speedometer`（速度表） |
| gauge-high | `fas:gauge-high` / `far:gauge-high` | `bs:speedometer`（速度表） |
| gauge-high-regular | `fas:gauge-high-regular` / `far:gauge-high-regular` | `bs:speedometer`（速度表） |
| gauge-simple | `fas:gauge-simple` / `far:gauge-simple` | `bs:speedometer`（速度表） |
| gauge-simple-high | `fas:gauge-simple-high` / `far:gauge-simple-high` | `bs:speedometer`（速度表） |
| gavel | `fas:gavel` / `far:gavel` | `bs:hammer`（锤子） |
| gear | `fas:gear` / `far:gear` | `bs:gear`（齿轮） |
| gears | `fas:gears` / `far:gears` | `bs:gear-wide-connected`（齿轮连线） |
| gem | `fas:gem` / `far:gem` | `bs:gem`（宝石） |
| ghost | `fas:ghost` / `far:ghost` | `bs:ghost`（） |
| gift | `fas:gift` / `far:gift` | `bs:gift`（） |
| gift-card | `fas:gift-card` / `far:gift-card` | `bs:gift`（） |
| glass-water | `fas:glass-water` / `far:glass-water` | `bs:droplet`（水滴） |
| glass-water-droplet | `fas:glass-water-droplet` / `far:glass-water-droplet` | `bs:droplet`（水滴） |
| glasses | `fas:glasses` / `far:glasses` | `bs:glasses`（） |
| globe | `fas:globe` / `far:globe` | `bs:globe-americas`（美洲地球） |
| globe-regular | `fas:globe-regular` / `far:globe-regular` | `bs:globe`（地球） |
| golf-ball-tee | `fas:golf-ball-tee` / `far:golf-ball-tee` | `bs:trophy`（奖杯） |
| graduation-cap | `fas:graduation-cap` / `far:graduation-cap` | `bs:mortarboard`（） |
| graduation-cap-regular | `fas:graduation-cap-regular` / `far:graduation-cap-regular` | `bs:mortarboard`（） |
| greater-than | `fas:greater-than` / `far:greater-than` | `bs:chevron-right`（右尖括号） |
| greater-than-equal | `fas:greater-than-equal` / `far:greater-than-equal` | `bs:chevron-right`（右尖括号） |
| guarani-sign | `fas:guarani-sign` / `far:guarani-sign` | `bs:currency-dollar`（美元） |
| guitar | `fas:guitar` / `far:guitar` | `bs:music-note`（音符） |
| hammer | `fas:hammer` / `far:hammer` | `bs:hammer`（锤子） |
| hand | `fas:hand` / `far:hand` | `bs:hand`（） |
| hand-fist | `fas:hand-fist` / `far:hand-fist` | `bs:hand`（） |
| hand-holding | `fas:hand-holding` / `far:hand-holding` | `bs:hand`（） |
| hand-holding-circle-dollar | `fas:hand-holding-circle-dollar` / `far:hand-holding-circle-dollar` | `bs:cash`（现金） |
| hand-holding-dollar | `fas:hand-holding-dollar` / `far:hand-holding-dollar` | `bs:cash`（现金） |
| hand-holding-droplet | `fas:hand-holding-droplet` / `far:hand-holding-droplet` | `bs:droplet`（水滴） |
| hand-holding-droplet-regular | `fas:hand-holding-droplet-regular` / `far:hand-holding-droplet-regular` | `bs:droplet`（水滴） |
| hand-holding-heart | `fas:hand-holding-heart` / `far:hand-holding-heart` | `bs:hand`（） |
| hand-holding-medical | `fas:hand-holding-medical` / `far:hand-holding-medical` | `bs:hand`（） |
| hand-holding-usd | `fas:hand-holding-usd` / `far:hand-holding-usd` | `bs:cash`（现金） |
| hand-holding-water | `fas:hand-holding-water` / `far:hand-holding-water` | `bs:droplet`（水滴） |
| hand-peace | `fas:hand-peace` / `far:hand-peace` | `bs:hand`（） |
| hand-point-left | `fas:hand-point-left` / `far:hand-point-left` | `bs:hand-index`（） |
| hand-point-right | `fas:hand-point-right` / `far:hand-point-right` | `bs:hand-index`（） |
| hand-pointer | `fas:hand-pointer` / `far:hand-pointer` | `bs:cursor`（） |
| hand-sparkles | `fas:hand-sparkles` / `far:hand-sparkles` | `bs:hand`（） |
| hands | `fas:hands` / `far:hands` | `bs:hand`（） |
| hands-bubbles | `fas:hands-bubbles` / `far:hands-bubbles` | `bs:hand`（） |
| handshake | `fas:handshake` / `far:handshake` | `bs:handshake`（） |
| hard-drive | `fas:hard-drive` / `far:hard-drive` | `bs:hdd`（） |
| hashtag | `fas:hashtag` / `far:hashtag` | `bs:hash`（） |
| hat-wizard | `fas:hat-wizard` / `far:hat-wizard` | `bs:magic`（） |
| head-side-mask | `fas:head-side-mask` / `far:head-side-mask` | `bs:mask`（） |
| headphones | `fas:headphones` / `far:headphones` | `bs:headset`（耳麦） |
| headphones-simple | `fas:headphones-simple` / `far:headphones-simple` | `bs:headset`（耳麦） |
| heart | `fas:heart` / `far:heart` | `bs:heart`（心形） |
| heart-crack | `fas:heart-crack` / `far:heart-crack` | `bs:heart`（心形） |
| heart-pulse | `fas:heart-pulse` / `far:heart-pulse` | `bs:activity`（脉搏） |
| heart-regular | `fas:heart-regular` / `far:heart-regular` | `bs:heart`（心形） |
| heartbeat | `fas:heartbeat` / `far:heartbeat` | `bs:activity`（脉搏） |
| helicopter | `fas:helicopter` / `far:helicopter` | `bs:send`（发送） |
| highway | `fas:highway` / `far:highway` | `bs:signpost`（） |
| hippo | `fas:hippo` / `far:hippo` | `bs:bug`（） |
| history | `fas:history` / `far:history` | `bs:clock-history`（历史时钟） |
| home | `fas:home` / `far:home` | `bs:house`（房屋） |
| home-regular | `fas:home-regular` / `far:home-regular` | `bs:house`（房屋） |
| horse | `fas:horse` / `far:horse` | `bs:horse`（） |
| hospital | `fas:hospital` / `far:hospital` | `bs:hospital`（） |
| hospital-user | `fas:hospital-user` / `far:hospital-user` | `bs:person-badge`（人员徽章） |
| hotdog | `fas:hotdog` / `far:hotdog` | `bs:egg`（） |
| hotel | `fas:hotel` / `far:hotel` | `bs:building`（大楼） |
| hotel-regular | `fas:hotel-regular` / `far:hotel-regular` | `bs:building`（大楼） |
| hourglass | `fas:hourglass` / `far:hourglass` | `bs:hourglass-split`（沙漏） |
| hourglass-end | `fas:hourglass-end` / `far:hourglass-end` | `bs:hourglass`（） |
| hourglass-half | `fas:hourglass-half` / `far:hourglass-half` | `bs:hourglass-split`（沙漏） |
| hourglass-start | `fas:hourglass-start` / `far:hourglass-start` | `bs:hourglass`（） |
| house | `fas:house` / `far:house` | `bs:house`（房屋） |
| house-chimney-medical | `fas:house-chimney-medical` / `far:house-chimney-medical` | `bs:hospital`（） |
| house-chimney-user | `fas:house-chimney-user` / `far:house-chimney-user` | `bs:house`（房屋） |
| house-chimney-window | `fas:house-chimney-window` / `far:house-chimney-window` | `bs:house`（房屋） |
| house-circle-check | `fas:house-circle-check` / `far:house-circle-check` | `bs:house`（房屋） |
| house-circle-exclamation | `fas:house-circle-exclamation` / `far:house-circle-exclamation` | `bs:house`（房屋） |
| house-circle-xmark | `fas:house-circle-xmark` / `far:house-circle-xmark` | `bs:house`（房屋） |
| house-crack | `fas:house-crack` / `far:house-crack` | `bs:house`（房屋） |
| house-crack-regular-regular | `fas:house-crack-regular-regular` / `far:house-crack-regular-regular` | `bs:house`（房屋） |
| house-fire | `fas:house-fire` / `far:house-fire` | `bs:house`（房屋） |
| house-flag | `fas:house-flag` / `far:house-flag` | `bs:house`（房屋） |
| house-laptop | `fas:house-laptop` / `far:house-laptop` | `bs:laptop`（笔记本） |
| house-lock | `fas:house-lock` / `far:house-lock` | `bs:house-lock`（） |
| house-medical-flag | `fas:house-medical-flag` / `far:house-medical-flag` | `bs:hospital`（） |
| house-regular | `fas:house-regular` / `far:house-regular` | `bs:house`（房屋） |
| house-signal | `fas:house-signal` / `far:house-signal` | `bs:wifi`（无线） |
| house-tree | `fas:house-tree` / `far:house-tree` | `bs:house`（房屋） |
| house-user | `fas:house-user` / `far:house-user` | `bs:house`（房屋） |
| hryvnia-sign | `fas:hryvnia-sign` / `far:hryvnia-sign` | `bs:currency-dollar`（美元） |
| hurricane | `fas:hurricane` / `far:hurricane` | `bs:tornado`（） |
| ice-cream | `fas:ice-cream` / `far:ice-cream` | `bs:egg`（） |
| icicles | `fas:icicles` / `far:icicles` | `bs:snow`（） |
| id-badge | `fas:id-badge` / `far:id-badge` | `bs:person-vcard`（） |
| id-card | `fas:id-card` / `far:id-card` | `bs:person-vcard`（） |
| image | `fas:image` / `far:image` | `bs:image`（） |
| images | `fas:images` / `far:images` | `bs:images`（图片集） |
| inbox | `fas:inbox` / `far:inbox` | `bs:inbox`（） |
| inbox-regular | `fas:inbox-regular` / `far:inbox-regular` | `bs:inbox`（） |
| inbox-regular-regular | `fas:inbox-regular-regular` / `far:inbox-regular-regular` | `bs:inbox`（） |
| indian-rupee-sign | `fas:indian-rupee-sign` / `far:indian-rupee-sign` | `bs:currency-rupee`（） |
| industry | `fas:industry` / `far:industry` | `bs:building`（大楼） |
| infinity | `fas:infinity` / `far:infinity` | `bs:infinity`（无限） |
| info | `fas:info` / `far:info` | `bs:info-lg`（） |
| info-regular | `fas:info-regular` / `far:info-regular` | `bs:info-lg`（） |
| jar | `fas:jar` / `far:jar` | `bs:cup`（） |
| jar-wheat | `fas:jar-wheat` / `far:jar-wheat` | `bs:cup`（） |
| jet-fighter | `fas:jet-fighter` / `far:jet-fighter` | `bs:send`（发送） |
| jug-detergent | `fas:jug-detergent` / `far:jug-detergent` | `bs:droplet`（水滴） |
| kaaba | `fas:kaaba` / `far:kaaba` | `bs:building`（大楼） |
| key | `fas:key` / `far:key` | `bs:key`（钥匙） |
| kip-sign | `fas:kip-sign` / `far:kip-sign` | `bs:currency-dollar`（美元） |
| kips-sign | `fas:kips-sign` / `far:kips-sign` | `bs:currency-dollar`（美元） |
| kit-medical | `fas:kit-medical` / `far:kit-medical` | `bs:first-aid`（） |
| kitchen-set | `fas:kitchen-set` / `far:kitchen-set` | `bs:cup`（） |
| landmark | `fas:landmark` / `far:landmark` | `bs:bank`（银行） |
| laptop | `fas:laptop` / `far:laptop` | `bs:laptop`（笔记本） |
| laptop-code | `fas:laptop-code` / `far:laptop-code` | `bs:laptop`（笔记本） |
| laptop-mobile | `fas:laptop-mobile` / `far:laptop-mobile` | `bs:laptop`（笔记本） |
| layer-group | `fas:layer-group` / `far:layer-group` | `bs:layers`（图层） |
| leaf | `fas:leaf` / `far:leaf` | `bs:leaf`（） |
| leafy-green | `fas:leafy-green` / `far:leafy-green` | `bs:flower1`（花朵） |
| left-from-bracket | `fas:left-from-bracket` / `far:left-from-bracket` | `bs:box-arrow-left`（） |
| left-right | `fas:left-right` / `far:left-right` | `bs:arrow-left-right`（） |
| left-to-bracket | `fas:left-to-bracket` / `far:left-to-bracket` | `bs:box-arrow-in-left`（） |
| legal | `fas:legal` / `far:legal` | `bs:bank`（银行） |
| less-than | `fas:less-than` / `far:less-than` | `bs:chevron-left`（左尖括号） |
| less-than-equal | `fas:less-than-equal` / `far:less-than-equal` | `bs:chevron-left`（左尖括号） |
| library | `fas:library` / `far:library` | `bs:journal-text`（日志） |
| life-ring | `fas:life-ring` / `far:life-ring` | `bs:life-preserver`（） |
| lightbulb | `fas:lightbulb` / `far:lightbulb` | `bs:lightbulb`（灯泡） |
| lightbulb-on | `fas:lightbulb-on` / `far:lightbulb-on` | `bs:lightbulb-fill`（灯泡实心） |
| lightbulb-regular | `fas:lightbulb-regular` / `far:lightbulb-regular` | `bs:lightbulb`（灯泡） |
| link | `fas:link` / `far:link` | `bs:link-45deg`（链接） |
| link-slash | `fas:link-slash` / `far:link-slash` | `bs:link`（） |
| link-slash-regular | `fas:link-slash-regular` / `far:link-slash-regular` | `bs:link`（） |
| lira-sign | `fas:lira-sign` / `far:lira-sign` | `bs:currency-dollar`（美元） |
| list | `fas:list` / `far:list` | `bs:list-ul`（列表） |
| list-ol | `fas:list-ol` / `far:list-ol` | `bs:list-ol`（） |
| list-ul | `fas:list-ul` / `far:list-ul` | `bs:list-ul`（列表） |
| location-arrow | `fas:location-arrow` / `far:location-arrow` | `bs:send`（发送） |
| location-crosshairs | `fas:location-crosshairs` / `far:location-crosshairs` | `bs:crosshair`（） |
| location-dot | `fas:location-dot` / `far:location-dot` | `bs:geo-alt`（定位针） |
| location-pin | `fas:location-pin` / `far:location-pin` | `bs:geo-alt`（定位针） |
| lock | `fas:lock` / `far:lock` | `bs:lock`（锁） |
| lock-open | `fas:lock-open` / `far:lock-open` | `bs:unlock`（开锁） |
| lollipop | `fas:lollipop` / `far:lollipop` | `bs:egg`（） |
| lungs | `fas:lungs` / `far:lungs` | `bs:lungs`（） |
| magic-wand-sparkles | `fas:magic-wand-sparkles` / `far:magic-wand-sparkles` | `bs:magic`（） |
| magnet | `fas:magnet` / `far:magnet` | `bs:magnet`（） |
| magnifying-glass | `fas:magnifying-glass` / `far:magnifying-glass` | `bs:search`（搜索） |
| manat-sign | `fas:manat-sign` / `far:manat-sign` | `bs:currency-dollar`（美元） |
| map | `fas:map` / `far:map` | `bs:map`（地图） |
| map-location | `fas:map-location` / `far:map-location` | `bs:geo-alt`（定位针） |
| map-location-dot | `fas:map-location-dot` / `far:map-location-dot` | `bs:geo-alt`（定位针） |
| map-pin | `fas:map-pin` / `far:map-pin` | `bs:pin-map`（地图针） |
| map-pin-regular | `fas:map-pin-regular` / `far:map-pin-regular` | `bs:pin-map`（地图针） |
| martini-glass | `fas:martini-glass` / `far:martini-glass` | `bs:cup`（） |
| mask-face | `fas:mask-face` / `far:mask-face` | `bs:mask`（） |
| maximize | `fas:maximize` / `far:maximize` | `bs:arrows-fullscreen`（） |
| medal | `fas:medal` / `far:medal` | `bs:award`（奖章） |
| medal-regular-regular-regular | `fas:medal-regular-regular-regular` / `far:medal-regular-regular-regular` | `bs:award`（奖章） |
| memory | `fas:memory` / `far:memory` | `bs:memory`（） |
| message | `fas:message` / `far:message` | `bs:chat-square-text`（文字气泡） |
| meteor | `fas:meteor` / `far:meteor` | `bs:star`（星形） |
| microchip | `fas:microchip` / `far:microchip` | `bs:cpu`（芯片） |
| microphone | `fas:microphone` / `far:microphone` | `bs:mic`（麦克风） |
| microphone-stand | `fas:microphone-stand` / `far:microphone-stand` | `bs:mic`（麦克风） |
| microscope | `fas:microscope` / `far:microscope` | `bs:microscope`（） |
| milestone | `fas:milestone` / `far:milestone` | `bs:signpost`（） |
| minimize | `fas:minimize` / `far:minimize` | `bs:arrows-fullscreen`（） |
| minus | `fas:minus` / `far:minus` | `bs:dash`（横杠） |
| mobile | `fas:mobile` / `far:mobile` | `bs:phone`（手机） |
| mobile-screen | `fas:mobile-screen` / `far:mobile-screen` | `bs:phone`（手机） |
| mobile-screen-button | `fas:mobile-screen-button` / `far:mobile-screen-button` | `bs:phone`（手机） |
| mobile-screen-regular | `fas:mobile-screen-regular` / `far:mobile-screen-regular` | `bs:phone`（手机） |
| money-bill | `fas:money-bill` / `far:money-bill` | `bs:cash`（现金） |
| money-bill-transfer | `fas:money-bill-transfer` / `far:money-bill-transfer` | `bs:cash`（现金） |
| money-bill-trend-up | `fas:money-bill-trend-up` / `far:money-bill-trend-up` | `bs:graph-up-arrow`（上升箭头图） |
| money-bill-wave | `fas:money-bill-wave` / `far:money-bill-wave` | `bs:cash`（现金） |
| money-bill-wave-regular | `fas:money-bill-wave-regular` / `far:money-bill-wave-regular` | `bs:cash`（现金） |
| money-bill-wheat | `fas:money-bill-wheat` / `far:money-bill-wheat` | `bs:cash`（现金） |
| money-check | `fas:money-check` / `far:money-check` | `bs:cash`（现金） |
| money-check-dollar | `fas:money-check-dollar` / `far:money-check-dollar` | `bs:cash`（现金） |
| money-check-dollar-regular | `fas:money-check-dollar-regular` / `far:money-check-dollar-regular` | `bs:cash`（现金） |
| money-check-regular | `fas:money-check-regular` / `far:money-check-regular` | `bs:cash`（现金） |
| moon | `fas:moon` / `far:moon` | `bs:moon`（月亮） |
| mosque | `fas:mosque` / `far:mosque` | `bs:building`（大楼） |
| motorcycle | `fas:motorcycle` / `far:motorcycle` | `bs:bicycle`（） |
| mound | `fas:mound` / `far:mound` | `bs:mountain`（） |
| mountain | `fas:mountain` / `far:mountain` | `bs:mountain`（） |
| mountain-city | `fas:mountain-city` / `far:mountain-city` | `bs:mountain`（） |
| mountain-regular-regular | `fas:mountain-regular-regular` / `far:mountain-regular-regular` | `bs:mountain`（） |
| mountain-sun-regular | `fas:mountain-sun-regular` / `far:mountain-sun-regular` | `bs:mountain`（） |
| mouse-pointer | `fas:mouse-pointer` / `far:mouse-pointer` | `bs:cursor`（） |
| mouse-regular | `fas:mouse-regular` / `far:mouse-regular` | `bs:mouse`（） |
| move | `fas:move` / `far:move` | `bs:arrows-move`（） |
| mug-hot | `fas:mug-hot` / `far:mug-hot` | `bs:cup-hot`（） |
| mug-marshmallows | `fas:mug-marshmallows` / `far:mug-marshmallows` | `bs:cup-hot`（） |
| mug-regular | `fas:mug-regular` / `far:mug-regular` | `bs:cup`（） |
| mug-regular-regular | `fas:mug-regular-regular` / `far:mug-regular-regular` | `bs:cup`（） |
| mug-saucer | `fas:mug-saucer` / `far:mug-saucer` | `bs:cup`（） |
| music | `fas:music` / `far:music` | `bs:music-note`（音符） |
| mute | `fas:mute` / `far:mute` | `bs:volume-mute`（） |
| network-wired | `fas:network-wired` / `far:network-wired` | `bs:diagram-3`（） |
| network-wired-regular | `fas:network-wired-regular` / `far:network-wired-regular` | `bs:diagram-3`（） |
| newspaper | `fas:newspaper` / `far:newspaper` | `bs:newspaper`（） |
| newspaper-regular | `fas:newspaper-regular` / `far:newspaper-regular` | `bs:newspaper`（） |
| nisisign | `fas:nisisign` / `far:nisisign` | `bs:currency-dollar`（美元） |
| no-smoking | `fas:no-smoking` / `far:no-smoking` | `bs:ban`（禁止） |
| not-equal | `fas:not-equal` / `far:not-equal` | `bs:not-equal`（） |
| oil-can | `fas:oil-can` / `far:oil-can` | `bs:fuel-pump`（） |
| outbox | `fas:outbox` / `far:outbox` | `bs:box-arrow-right`（） |
| paint-roller | `fas:paint-roller` / `far:paint-roller` | `bs:brush`（） |
| paintbrush | `fas:paintbrush` / `far:paintbrush` | `bs:brush`（） |
| palette | `fas:palette` / `far:palette` | `bs:palette`（） |
| palm-tree | `fas:palm-tree` / `far:palm-tree` | `bs:tree`（） |
| palm-tree-regular | `fas:palm-tree-regular` / `far:palm-tree-regular` | `bs:tree`（） |
| paper-plane | `fas:paper-plane` / `far:paper-plane` | `bs:send`（发送） |
| paper-plane-regular | `fas:paper-plane-regular` / `far:paper-plane-regular` | `bs:send`（发送） |
| paperclip | `fas:paperclip` / `far:paperclip` | `bs:paperclip`（） |
| paperclip-vertical | `fas:paperclip-vertical` / `far:paperclip-vertical` | `bs:paperclip`（） |
| parachute-box | `fas:parachute-box` / `far:parachute-box` | `bs:box`（盒子） |
| passport | `fas:passport` / `far:passport` | `bs:person-vcard`（） |
| paste | `fas:paste` / `far:paste` | `bs:clipboard-data`（剪贴板数据） |
| pause | `fas:pause` / `far:pause` | `bs:pause-circle`（暂停） |
| pause-regular | `fas:pause-regular` / `far:pause-regular` | `bs:pause-circle`（暂停） |
| paw | `fas:paw` / `far:paw` | `bs:paw`（） |
| pen | `fas:pen` / `far:pen` | `bs:pencil`（铅笔） |
| pen-clip | `fas:pen-clip` / `far:pen-clip` | `bs:pencil`（铅笔） |
| pen-fancy | `fas:pen-fancy` / `far:pen-fancy` | `bs:pen`（） |
| pen-nib | `fas:pen-nib` / `far:pen-nib` | `bs:pen`（） |
| pen-regular | `fas:pen-regular` / `far:pen-regular` | `bs:pencil`（铅笔） |
| pen-ruler | `fas:pen-ruler` / `far:pen-ruler` | `bs:pencil-square`（） |
| pen-to-square | `fas:pen-to-square` / `far:pen-to-square` | `bs:pencil-square`（） |
| pencil | `fas:pencil` / `far:pencil` | `bs:pencil`（铅笔） |
| pencil-regular | `fas:pencil-regular` / `far:pencil-regular` | `bs:pencil`（铅笔） |
| pencil-ruler | `fas:pencil-ruler` / `far:pencil-ruler` | `bs:pencil-square`（） |
| people-arrows | `fas:people-arrows` / `far:people-arrows` | `bs:people`（多人） |
| people-carry-box | `fas:people-carry-box` / `far:people-carry-box` | `bs:people`（多人） |
| people-group | `fas:people-group` / `far:people-group` | `bs:people`（多人） |
| people-line | `fas:people-line` / `far:people-line` | `bs:people`（多人） |
| people-pulling | `fas:people-pulling` / `far:people-pulling` | `bs:people`（多人） |
| people-robbery | `fas:people-robbery` / `far:people-robbery` | `bs:people`（多人） |
| people-roof | `fas:people-roof` / `far:people-roof` | `bs:people`（多人） |
| pepper-hot | `fas:pepper-hot` / `far:pepper-hot` | `bs:egg`（） |
| percent | `fas:percent` / `far:percent` | `bs:percent`（百分号） |
| person-arrow-down-right-from-square | `fas:person-arrow-down-right-from-square` / `far:person-arrow-down-right-from-square` | `bs:person`（单人） |
| person-arrow-down-to-line | `fas:person-arrow-down-to-line` / `far:person-arrow-down-to-line` | `bs:person`（单人） |
| person-arrow-up-from-line | `fas:person-arrow-up-from-line` / `far:person-arrow-up-from-line` | `bs:person`（单人） |
| person-arrow-up-right-from-square | `fas:person-arrow-up-right-from-square` / `far:person-arrow-up-right-from-square` | `bs:person`（单人） |
| person-biking | `fas:person-biking` / `far:person-biking` | `bs:bicycle`（） |
| person-booth | `fas:person-booth` / `far:person-booth` | `bs:person`（单人） |
| person-cane | `fas:person-cane` / `far:person-cane` | `bs:person`（单人） |
| person-carry-box | `fas:person-carry-box` / `far:person-carry-box` | `bs:person`（单人） |
| person-chalkboard | `fas:person-chalkboard` / `far:person-chalkboard` | `bs:person-badge`（人员徽章） |
| person-chalkboard-regular | `fas:person-chalkboard-regular` / `far:person-chalkboard-regular` | `bs:person-badge`（人员徽章） |
| person-circle-check | `fas:person-circle-check` / `far:person-circle-check` | `bs:person-check`（人员对勾） |
| person-circle-info | `fas:person-circle-info` / `far:person-circle-info` | `bs:person`（单人） |
| person-circle-minus | `fas:person-circle-minus` / `far:person-circle-minus` | `bs:person-dash`（） |
| person-circle-minus-regular | `fas:person-circle-minus-regular` / `far:person-circle-minus-regular` | `bs:person-dash`（） |
| person-circle-plus | `fas:person-circle-plus` / `far:person-circle-plus` | `bs:person-plus`（添加人员） |
| person-circle-xmark | `fas:person-circle-xmark` / `far:person-circle-xmark` | `bs:person-x`（） |
| person-digging | `fas:person-digging` / `far:person-digging` | `bs:person-walking`（） |
| person-dolly-empty | `fas:person-dolly-empty` / `far:person-dolly-empty` | `bs:person`（单人） |
| person-dots-from-line | `fas:person-dots-from-line` / `far:person-dots-from-line` | `bs:person`（单人） |
| person-dress | `fas:person-dress` / `far:person-dress` | `bs:person`（单人） |
| person-dress-burst | `fas:person-dress-burst` / `far:person-dress-burst` | `bs:person`（单人） |
| person-exclamation | `fas:person-exclamation` / `far:person-exclamation` | `bs:person`（单人） |
| person-falling | `fas:person-falling` / `far:person-falling` | `bs:person`（单人） |
| person-falling-burst | `fas:person-falling-burst` / `far:person-falling-burst` | `bs:person`（单人） |
| person-ferry | `fas:person-ferry` / `far:person-ferry` | `bs:ship`（） |
| person-half-dress | `fas:person-half-dress` / `far:person-half-dress` | `bs:person`（单人） |
| person-harassing | `fas:person-harassing` / `far:person-harassing` | `bs:person`（单人） |
| person-hiking | `fas:person-hiking` / `far:person-hiking` | `bs:person-walking`（） |
| person-military-pointing | `fas:person-military-pointing` / `far:person-military-pointing` | `bs:person`（单人） |
| person-military-rifle | `fas:person-military-rifle` / `far:person-military-rifle` | `bs:person`（单人） |
| person-military-to-person | `fas:person-military-to-person` / `far:person-military-to-person` | `bs:person`（单人） |
| person-pin | `fas:person-pin` / `far:person-pin` | `bs:person`（单人） |
| person-rays | `fas:person-rays` / `far:person-rays` | `bs:person`（单人） |
| person-rifle | `fas:person-rifle` / `far:person-rifle` | `bs:person`（单人） |
| person-running | `fas:person-running` / `far:person-running` | `bs:person-walking`（） |
| person-seat | `fas:person-seat` / `far:person-seat` | `bs:person`（单人） |
| person-seat-reclined | `fas:person-seat-reclined` / `far:person-seat-reclined` | `bs:person`（单人） |
| person-shelter | `fas:person-shelter` / `far:person-shelter` | `bs:person`（单人） |
| person-sign | `fas:person-sign` / `far:person-sign` | `bs:person`（单人） |
| person-simple | `fas:person-simple` / `far:person-simple` | `bs:person`（单人） |
| person-skating | `fas:person-skating` / `far:person-skating` | `bs:person-walking`（） |
| person-skiing | `fas:person-skiing` / `far:person-skiing` | `bs:person-walking`（） |
| person-skiing-nordic | `fas:person-skiing-nordic` / `far:person-skiing-nordic` | `bs:person-walking`（） |
| person-skiing-slalom | `fas:person-skiing-slalom` / `far:person-skiing-slalom` | `bs:person-walking`（） |
| person-skull | `fas:person-skull` / `far:person-skull` | `bs:skull`（） |
| person-snowboarding | `fas:person-snowboarding` / `far:person-snowboarding` | `bs:person-walking`（） |
| person-snowmobiling | `fas:person-snowmobiling` / `far:person-snowmobiling` | `bs:person-walking`（） |
| person-stairs | `fas:person-stairs` / `far:person-stairs` | `bs:person`（单人） |
| person-standing | `fas:person-standing` / `far:person-standing` | `bs:person`（单人） |
| person-swimming | `fas:person-swimming` / `far:person-swimming` | `bs:person-walking`（） |
| person-through-window | `fas:person-through-window` / `far:person-through-window` | `bs:person`（单人） |
| person-walking | `fas:person-walking` / `far:person-walking` | `bs:person-walking`（） |
| person-walking-arrow-loop-left | `fas:person-walking-arrow-loop-left` / `far:person-walking-arrow-loop-left` | `bs:person-walking`（） |
| person-walking-arrow-right | `fas:person-walking-arrow-right` / `far:person-walking-arrow-right` | `bs:person-walking`（） |
| person-walking-dashed-line-arrow-right | `fas:person-walking-dashed-line-arrow-right` / `far:person-walking-dashed-line-arrow-right` | `bs:person-walking`（） |
| person-walking-luggage | `fas:person-walking-luggage` / `far:person-walking-luggage` | `bs:person-walking`（） |
| person-wheelchair-move | `fas:person-wheelchair-move` / `far:person-wheelchair-move` | `bs:person-wheelchair`（） |
| phone | `fas:phone` / `far:phone` | `bs:telephone`（电话） |
| phone-arrow-down-left | `fas:phone-arrow-down-left` / `far:phone-arrow-down-left` | `bs:telephone-inbound`（） |
| phone-arrow-up-right | `fas:phone-arrow-up-right` / `far:phone-arrow-up-right` | `bs:telephone-outbound`（） |
| phone-missed | `fas:phone-missed` / `far:phone-missed` | `bs:telephone`（电话） |
| phone-office | `fas:phone-office` / `far:phone-office` | `bs:telephone`（电话） |
| phone-slash | `fas:phone-slash` / `far:phone-slash` | `bs:telephone-x`（） |
| phone-volume | `fas:phone-volume` / `far:phone-volume` | `bs:telephone`（电话） |
| phone-xmark | `fas:phone-xmark` / `far:phone-xmark` | `bs:telephone-x`（） |
| piano | `fas:piano` / `far:piano` | `bs:music-note`（音符） |
| pill | `fas:pill` / `far:pill` | `bs:capsule`（） |
| pills | `fas:pills` / `far:pills` | `bs:capsule`（） |
| pipette | `fas:pipette` / `far:pipette` | `bs:eyedropper`（） |
| pizza-slice | `fas:pizza-slice` / `far:pizza-slice` | `bs:egg`（） |
| plane | `fas:plane` / `far:plane` | `bs:send`（发送） |
| plate-wheat | `fas:plate-wheat` / `far:plate-wheat` | `bs:egg`（） |
| plate-wheat-regular | `fas:plate-wheat-regular` / `far:plate-wheat-regular` | `bs:egg`（） |
| play-regular | `fas:play-regular` / `far:play-regular` | `bs:play-circle`（播放） |
| plug | `fas:plug` / `far:plug` | `bs:plug`（） |
| plug-circle-bolt | `fas:plug-circle-bolt` / `far:plug-circle-bolt` | `bs:plug`（） |
| plug-circle-check | `fas:plug-circle-check` / `far:plug-circle-check` | `bs:plug`（） |
| plug-circle-minus | `fas:plug-circle-minus` / `far:plug-circle-minus` | `bs:plug`（） |
| plug-circle-plus | `fas:plug-circle-plus` / `far:plug-circle-plus` | `bs:plug`（） |
| plug-circle-xmark | `fas:plug-circle-xmark` / `far:plug-circle-xmark` | `bs:plug`（） |
| plug-circle-xmark-regular | `fas:plug-circle-xmark-regular` / `far:plug-circle-xmark-regular` | `bs:plug`（） |
| plus | `fas:plus` / `far:plus` | `bs:plus`（加号） |
| plus-minus | `fas:plus-minus` / `far:plus-minus` | `bs:plus`（加号） |
| podcast | `fas:podcast` / `far:podcast` | `bs:mic`（麦克风） |
| point-up | `fas:point-up` / `far:point-up` | `bs:hand-index`（） |
| pointer | `fas:pointer` / `far:pointer` | `bs:cursor`（） |
| popcorn | `fas:popcorn` / `far:popcorn` | `bs:egg`（） |
| pound-sign | `fas:pound-sign` / `far:pound-sign` | `bs:currency-pound`（） |
| power-off | `fas:power-off` / `far:power-off` | `bs:power`（） |
| power-off-regular | `fas:power-off-regular` / `far:power-off-regular` | `bs:power`（） |
| prescription | `fas:prescription` / `far:prescription` | `bs:capsule`（） |
| prescription-bottle | `fas:prescription-bottle` / `far:prescription-bottle` | `bs:capsule`（） |
| prescription-bottle-medical | `fas:prescription-bottle-medical` / `far:prescription-bottle-medical` | `bs:capsule`（） |
| print | `fas:print` / `far:print` | `bs:printer`（打印机） |
| print-regular | `fas:print-regular` / `far:print-regular` | `bs:printer`（打印机） |
| print-regular-regular | `fas:print-regular-regular` / `far:print-regular-regular` | `bs:printer`（打印机） |
| project-diagram | `fas:project-diagram` / `far:project-diagram` | `bs:diagram-3`（） |
| projector | `fas:projector` / `far:projector` | `bs:display`（显示器） |
| pulse | `fas:pulse` / `far:pulse` | `bs:activity`（脉搏） |
| pump-soap | `fas:pump-soap` / `far:pump-soap` | `bs:droplet`（水滴） |
| puzzle-piece | `fas:puzzle-piece` / `far:puzzle-piece` | `bs:puzzle`（） |
| qr-code | `fas:qr-code` / `far:qr-code` | `bs:qr-code`（） |
| question | `fas:question` / `far:question` | `bs:question-lg`（） |
| question-regular | `fas:question-regular` / `far:question-regular` | `bs:question-lg`（） |
| quote-left | `fas:quote-left` / `far:quote-left` | `bs:quote`（） |
| quote-right | `fas:quote-right` / `far:quote-right` | `bs:quote`（） |
| radio | `fas:radio` / `far:radio` | `bs:broadcast`（） |
| radiology | `fas:radiology` / `far:radiology` | `bs:activity`（脉搏） |
| rainbow | `fas:rainbow` / `far:rainbow` | `bs:rainbow`（） |
| record-regular | `fas:record-regular` / `far:record-regular` | `bs:record-circle`（） |
| record-vinyl | `fas:record-vinyl` / `far:record-vinyl` | `bs:record-circle`（） |
| recycle | `fas:recycle` / `far:recycle` | `bs:arrow-repeat`（循环） |
| refresh | `fas:refresh` / `far:refresh` | `bs:arrow-repeat`（循环） |
| reply-all | `fas:reply-all` / `far:reply-all` | `bs:reply-all`（） |
| reply-regular | `fas:reply-regular` / `far:reply-regular` | `bs:reply`（回复） |
| retweet | `fas:retweet` / `far:retweet` | `bs:arrow-repeat`（循环） |
| rial-sign | `fas:rial-sign` / `far:rial-sign` | `bs:currency-dollar`（美元） |
| right-from-bracket | `fas:right-from-bracket` / `far:right-from-bracket` | `bs:box-arrow-right`（） |
| right-left | `fas:right-left` / `far:right-left` | `bs:arrow-left-right`（） |
| right-to-bracket | `fas:right-to-bracket` / `far:right-to-bracket` | `bs:box-arrow-in-right`（） |
| road | `fas:road` / `far:road` | `bs:signpost`（） |
| robot | `fas:robot` / `far:robot` | `bs:robot`（） |
| rocket | `fas:rocket` / `far:rocket` | `bs:rocket-takeoff`（火箭发射） |
| rocket-regular | `fas:rocket-regular` / `far:rocket-regular` | `bs:rocket-takeoff`（火箭发射） |
| rotate | `fas:rotate` / `far:rotate` | `bs:arrow-repeat`（循环） |
| rotate-left | `fas:rotate-left` / `far:rotate-left` | `bs:arrow-counterclockwise`（） |
| rotate-right | `fas:rotate-right` / `far:rotate-right` | `bs:arrow-clockwise`（） |
| route | `fas:route` / `far:route` | `bs:signpost`（） |
| route-regular | `fas:route-regular` / `far:route-regular` | `bs:signpost`（） |
| rss | `fas:rss` / `far:rss` | `bs:rss`（） |
| ruble-sign | `fas:ruble-sign` / `far:ruble-sign` | `bs:currency-dollar`（美元） |
| rugby | `fas:rugby` / `far:rugby` | `bs:trophy`（奖杯） |
| ruler | `fas:ruler` / `far:ruler` | `bs:ruler`（） |
| ruler-combined | `fas:ruler-combined` / `far:ruler-combined` | `bs:ruler`（） |
| ruler-horizontal | `fas:ruler-horizontal` / `far:ruler-horizontal` | `bs:ruler`（） |
| ruler-vertical | `fas:ruler-vertical` / `far:ruler-vertical` | `bs:ruler`（） |
| rupee-sign | `fas:rupee-sign` / `far:rupee-sign` | `bs:currency-rupee`（） |
| sack-dollar | `fas:sack-dollar` / `far:sack-dollar` | `bs:cash-stack`（现金堆） |
| sailboat | `fas:sailboat` / `far:sailboat` | `bs:ship`（） |
| satellite | `fas:satellite` / `far:satellite` | `bs:broadcast`（） |
| satellite-dish | `fas:satellite-dish` / `far:satellite-dish` | `bs:broadcast`（） |
| save | `fas:save` / `far:save` | `bs:save`（） |
| saxophone | `fas:saxophone` / `far:saxophone` | `bs:music-note`（音符） |
| scale-balanced | `fas:scale-balanced` / `far:scale-balanced` | `bs:bank`（银行） |
| school | `fas:school` / `far:school` | `bs:mortarboard`（） |
| school-circle-exclamation | `fas:school-circle-exclamation` / `far:school-circle-exclamation` | `bs:mortarboard`（） |
| school-circle-xmark | `fas:school-circle-xmark` / `far:school-circle-xmark` | `bs:mortarboard`（） |
| school-flag | `fas:school-flag` / `far:school-flag` | `bs:mortarboard`（） |
| school-lock-regular | `fas:school-lock-regular` / `far:school-lock-regular` | `bs:mortarboard`（） |
| scooter | `fas:scooter` / `far:scooter` | `bs:bicycle`（） |
| screwdriver | `fas:screwdriver` / `far:screwdriver` | `bs:tools`（工具套装） |
| screwdriver-wrench | `fas:screwdriver-wrench` / `far:screwdriver-wrench` | `bs:tools`（工具套装） |
| search | `fas:search` / `far:search` | `bs:search`（搜索） |
| seedling | `fas:seedling` / `far:seedling` | `bs:flower1`（花朵） |
| seedling-regular-regular | `fas:seedling-regular-regular` / `far:seedling-regular-regular` | `bs:flower1`（花朵） |
| send-regular | `fas:send-regular` / `far:send-regular` | `bs:send`（发送） |
| server | `fas:server` / `far:server` | `bs:server`（） |
| shapes | `fas:shapes` / `far:shapes` | `bs:shapes`（） |
| share | `fas:share` / `far:share` | `bs:share`（分享） |
| share-nodes | `fas:share-nodes` / `far:share-nodes` | `bs:share`（分享） |
| share-square | `fas:share-square` / `far:share-square` | `bs:share`（分享） |
| shekel-sign | `fas:shekel-sign` / `far:shekel-sign` | `bs:currency-dollar`（美元） |
| shield | `fas:shield` / `far:shield` | `bs:shield-check`（盾牌对勾） |
| shield-halved | `fas:shield-halved` / `far:shield-halved` | `bs:shield-shaded`（盾牌） |
| shield-virus | `fas:shield-virus` / `far:shield-virus` | `bs:shield-check`（盾牌对勾） |
| ship | `fas:ship` / `far:ship` | `bs:ship`（） |
| shooting-star | `fas:shooting-star` / `far:shooting-star` | `bs:star`（星形） |
| shooting-star-regular | `fas:shooting-star-regular` / `far:shooting-star-regular` | `bs:star`（星形） |
| shop | `fas:shop` / `far:shop` | `bs:shop`（） |
| shower | `fas:shower` / `far:shower` | `bs:droplet`（水滴） |
| shrimp | `fas:shrimp` / `far:shrimp` | `bs:bug`（） |
| shuttle-space | `fas:shuttle-space` / `far:shuttle-space` | `bs:rocket-takeoff`（火箭发射） |
| sign | `fas:sign` / `far:sign` | `bs:signpost`（） |
| sign-hanging | `fas:sign-hanging` / `far:sign-hanging` | `bs:signpost`（） |
| sign-hanging-regular | `fas:sign-hanging-regular` / `far:sign-hanging-regular` | `bs:signpost`（） |
| sign-in | `fas:sign-in` / `far:sign-in` | `bs:box-arrow-in-right`（） |
| sign-in-alt | `fas:sign-in-alt` / `far:sign-in-alt` | `bs:box-arrow-in-right`（） |
| sign-out | `fas:sign-out` / `far:sign-out` | `bs:box-arrow-right`（） |
| sign-out-alt | `fas:sign-out-alt` / `far:sign-out-alt` | `bs:box-arrow-right`（） |
| signal | `fas:signal` / `far:signal` | `bs:wifi`（无线） |
| signal-strong | `fas:signal-strong` / `far:signal-strong` | `bs:wifi`（无线） |
| signs-post | `fas:signs-post` / `far:signs-post` | `bs:signpost`（） |
| signs-post-regular | `fas:signs-post-regular` / `far:signs-post-regular` | `bs:signpost`（） |
| signs-post-regular-regular | `fas:signs-post-regular-regular` / `far:signs-post-regular-regular` | `bs:signpost`（） |
| sitemap | `fas:sitemap` / `far:sitemap` | `bs:diagram-3`（） |
| sitemap-regular | `fas:sitemap-regular` / `far:sitemap-regular` | `bs:diagram-3`（） |
| skull | `fas:skull` / `far:skull` | `bs:skull`（） |
| sleigh | `fas:sleigh` / `far:sleigh` | `bs:snow`（） |
| sliders | `fas:sliders` / `far:sliders` | `bs:sliders`（调节滑杆） |
| smog | `fas:smog` / `far:smog` | `bs:cloud-fog2`（） |
| smoking | `fas:smoking` / `far:smoking` | `bs:cloud-fog2`（） |
| smoking-ban | `fas:smoking-ban` / `far:smoking-ban` | `bs:ban`（禁止） |
| snowflake | `fas:snowflake` / `far:snowflake` | `bs:snow`（） |
| snowman | `fas:snowman` / `far:snowman` | `bs:snow`（） |
| soap | `fas:soap` / `far:soap` | `bs:droplet`（水滴） |
| spa | `fas:spa` / `far:spa` | `bs:flower1`（花朵） |
| space-shuttle | `fas:space-shuttle` / `far:space-shuttle` | `bs:rocket-takeoff`（火箭发射） |
| speaker | `fas:speaker` / `far:speaker` | `bs:volume-up`（音量） |
| speaker-regular | `fas:speaker-regular` / `far:speaker-regular` | `bs:volume-up`（音量） |
| spider | `fas:spider` / `far:spider` | `bs:spider`（） |
| spray-can | `fas:spray-can` / `far:spray-can` | `bs:brush`（） |
| spruce | `fas:spruce` / `far:spruce` | `bs:tree`（） |
| square | `fas:square` / `far:square` | `bs:square`（） |
| square-caret-down | `fas:square-caret-down` / `far:square-caret-down` | `bs:caret-down-fill`（） |
| square-caret-left | `fas:square-caret-left` / `far:square-caret-left` | `bs:caret-left-fill`（） |
| square-caret-right | `fas:square-caret-right` / `far:square-caret-right` | `bs:caret-right-fill`（） |
| square-caret-up | `fas:square-caret-up` / `far:square-caret-up` | `bs:caret-up-fill`（） |
| square-check | `fas:square-check` / `far:square-check` | `bs:check-square`（） |
| square-check-regular | `fas:square-check-regular` / `far:square-check-regular` | `bs:check-square`（） |
| square-full | `fas:square-full` / `far:square-full` | `bs:square-fill`（） |
| square-full-regular | `fas:square-full-regular` / `far:square-full-regular` | `bs:square-fill`（） |
| square-h | `fas:square-h` / `far:square-h` | `bs:hospital`（） |
| square-heart | `fas:square-heart` / `far:square-heart` | `bs:heart`（心形） |
| square-minus | `fas:square-minus` / `far:square-minus` | `bs:dash-square`（） |
| square-person-confined | `fas:square-person-confined` / `far:square-person-confined` | `bs:person`（单人） |
| square-plus | `fas:square-plus` / `far:square-plus` | `bs:plus-square`（） |
| square-poll-horizontal | `fas:square-poll-horizontal` / `far:square-poll-horizontal` | `bs:bar-chart`（柱状图） |
| square-poll-vertical | `fas:square-poll-vertical` / `far:square-poll-vertical` | `bs:bar-chart`（柱状图） |
| square-regular-regular-regular | `fas:square-regular-regular-regular` / `far:square-regular-regular-regular` | `bs:square`（） |
| square-root-variable | `fas:square-root-variable` / `far:square-root-variable` | `bs:percent`（百分号） |
| square-xmark | `fas:square-xmark` / `far:square-xmark` | `bs:x-square`（） |
| staff-snake | `fas:staff-snake` / `far:staff-snake` | `bs:activity`（脉搏） |
| star | `fas:star` / `far:star` | `bs:star`（星形） |
| star-half-regular | `fas:star-half-regular` / `far:star-half-regular` | `bs:star-half`（半星） |
| star-half-stroke | `fas:star-half-stroke` / `far:star-half-stroke` | `bs:star-half`（半星） |
| star-of-david | `fas:star-of-david` / `far:star-of-david` | `bs:star-half`（半星） |
| star-of-life | `fas:star-of-life` / `far:star-of-life` | `bs:star`（星形） |
| star-regular | `fas:star-regular` / `far:star-regular` | `bs:star`（星形） |
| stethoscope | `fas:stethoscope` / `far:stethoscope` | `bs:heart-pulse`（） |
| stop | `fas:stop` / `far:stop` | `bs:stop-circle`（停止） |
| stop-regular | `fas:stop-regular` / `far:stop-regular` | `bs:stop-circle`（停止） |
| stopwatch | `fas:stopwatch` / `far:stopwatch` | `bs:stopwatch`（秒表） |
| store | `fas:store` / `far:store` | `bs:shop`（） |
| suitcase | `fas:suitcase` / `far:suitcase` | `bs:briefcase`（） |
| suitcase-rolling | `fas:suitcase-rolling` / `far:suitcase-rolling` | `bs:briefcase`（） |
| sun | `fas:sun` / `far:sun` | `bs:sun`（太阳） |
| swatchbook | `fas:swatchbook` / `far:swatchbook` | `bs:palette`（） |
| synagogue | `fas:synagogue` / `far:synagogue` | `bs:building`（大楼） |
| syringe | `fas:syringe` / `far:syringe` | `bs:heart-pulse`（） |
| syringe-regular | `fas:syringe-regular` / `far:syringe-regular` | `bs:heart-pulse`（） |
| table | `fas:table` / `far:table` | `bs:table`（） |
| table-cells | `fas:table-cells` / `far:table-cells` | `bs:table`（） |
| table-tennis-paddle-ball | `fas:table-tennis-paddle-ball` / `far:table-tennis-paddle-ball` | `bs:trophy`（奖杯） |
| tablet-regular | `fas:tablet-regular` / `far:tablet-regular` | `bs:tablet`（平板） |
| tablet-screen | `fas:tablet-screen` / `far:tablet-screen` | `bs:tablet`（平板） |
| tablet-screen-button | `fas:tablet-screen-button` / `far:tablet-screen-button` | `bs:tablet`（平板） |
| tablets | `fas:tablets` / `far:tablets` | `bs:capsule`（） |
| tachometer | `fas:tachometer` / `far:tachometer` | `bs:speedometer`（速度表） |
| tag | `fas:tag` / `far:tag` | `bs:tag`（） |
| tags | `fas:tags` / `far:tags` | `bs:tag`（） |
| taxi | `fas:taxi` / `far:taxi` | `bs:car-front`（） |
| temperature-half | `fas:temperature-half` / `far:temperature-half` | `bs:thermometer-half`（） |
| temperature-high | `fas:temperature-high` / `far:temperature-high` | `bs:thermometer-high`（） |
| temperature-low | `fas:temperature-low` / `far:temperature-low` | `bs:thermometer-low`（） |
| tenge-sign | `fas:tenge-sign` / `far:tenge-sign` | `bs:currency-dollar`（美元） |
| tennis-ball | `fas:tennis-ball` / `far:tennis-ball` | `bs:trophy`（奖杯） |
| tent | `fas:tent` / `far:tent` | `bs:moon-stars`（星月） |
| tent-arrow-left-right | `fas:tent-arrow-left-right` / `far:tent-arrow-left-right` | `bs:moon-stars`（星月） |
| tent-arrow-turn-left | `fas:tent-arrow-turn-left` / `far:tent-arrow-turn-left` | `bs:moon-stars`（星月） |
| tent-arrows-down | `fas:tent-arrows-down` / `far:tent-arrows-down` | `bs:moon-stars`（星月） |
| tent-regular | `fas:tent-regular` / `far:tent-regular` | `bs:moon-stars`（星月） |
| tents | `fas:tents` / `far:tents` | `bs:moon-stars`（星月） |
| tents-regular | `fas:tents-regular` / `far:tents-regular` | `bs:moon-stars`（星月） |
| terminal | `fas:terminal` / `far:terminal` | `bs:terminal`（） |
| test-tube | `fas:test-tube` / `far:test-tube` | `bs:flask`（） |
| test-tubes | `fas:test-tubes` / `far:test-tubes` | `bs:flask`（） |
| thermal | `fas:thermal` / `far:thermal` | `bs:thermometer-half`（） |
| thermometer | `fas:thermometer` / `far:thermometer` | `bs:thermometer-half`（） |
| thermometer-empty | `fas:thermometer-empty` / `far:thermometer-empty` | `bs:thermometer`（） |
| thermometer-full | `fas:thermometer-full` / `far:thermometer-full` | `bs:thermometer-high`（） |
| thermometer-half-regular | `fas:thermometer-half-regular` / `far:thermometer-half-regular` | `bs:thermometer-half`（） |
| thermometer-quarter | `fas:thermometer-quarter` / `far:thermometer-quarter` | `bs:thermometer-quarter`（） |
| thermometer-three-quarters | `fas:thermometer-three-quarters` / `far:thermometer-three-quarters` | `bs:thermometer-three-quarters`（） |
| thumbs-down | `fas:thumbs-down` / `far:thumbs-down` | `bs:hand-thumbs-down`（点踩） |
| thumbs-up | `fas:thumbs-up` / `far:thumbs-up` | `bs:hand-thumbs-up`（点赞） |
| ticket | `fas:ticket` / `far:ticket` | `bs:ticket-perforated`（） |
| timer | `fas:timer` / `far:timer` | `bs:stopwatch`（秒表） |
| timer-regular | `fas:timer-regular` / `far:timer-regular` | `bs:stopwatch`（秒表） |
| tint-slash | `fas:tint-slash` / `far:tint-slash` | `bs:droplet`（水滴） |
| toggle-off | `fas:toggle-off` / `far:toggle-off` | `bs:toggle-off`（） |
| toggle-on | `fas:toggle-on` / `far:toggle-on` | `bs:toggle-on`（） |
| toilet | `fas:toilet` / `far:toilet` | `bs:emoji-laughing`（） |
| tooth | `fas:tooth` / `far:tooth` | `bs:tooth`（） |
| tornado | `fas:tornado` / `far:tornado` | `bs:tornado`（） |
| tower-broadcast | `fas:tower-broadcast` / `far:tower-broadcast` | `bs:broadcast`（） |
| tower-cell | `fas:tower-cell` / `far:tower-cell` | `bs:broadcast`（） |
| tower-control | `fas:tower-control` / `far:tower-control` | `bs:broadcast`（） |
| traffic-light | `fas:traffic-light` / `far:traffic-light` | `bs:stoplight`（） |
| traffic-light-go | `fas:traffic-light-go` / `far:traffic-light-go` | `bs:stoplight`（） |
| traffic-light-regular-regular | `fas:traffic-light-regular-regular` / `far:traffic-light-regular-regular` | `bs:stoplight`（） |
| train | `fas:train` / `far:train` | `bs:train-front`（） |
| trash | `fas:trash` / `far:trash` | `bs:trash`（垃圾桶） |
| trash-arrow-down | `fas:trash-arrow-down` / `far:trash-arrow-down` | `bs:trash`（垃圾桶） |
| trash-can | `fas:trash-can` / `far:trash-can` | `bs:trash`（垃圾桶） |
| trash-can-arrow-down | `fas:trash-can-arrow-down` / `far:trash-can-arrow-down` | `bs:trash`（垃圾桶） |
| trash-can-arrow-up | `fas:trash-can-arrow-up` / `far:trash-can-arrow-up` | `bs:trash`（垃圾桶） |
| trash-can-clock | `fas:trash-can-clock` / `far:trash-can-clock` | `bs:trash`（垃圾桶） |
| trash-can-undo | `fas:trash-can-undo` / `far:trash-can-undo` | `bs:trash`（垃圾桶） |
| trash-can-xmark | `fas:trash-can-xmark` / `far:trash-can-xmark` | `bs:trash`（垃圾桶） |
| trash-clock | `fas:trash-clock` / `far:trash-clock` | `bs:trash`（垃圾桶） |
| trash-undo | `fas:trash-undo` / `far:trash-undo` | `bs:trash`（垃圾桶） |
| tray-arrow-down | `fas:tray-arrow-down` / `far:tray-arrow-down` | `bs:download`（下载） |
| tray-arrow-up | `fas:tray-arrow-up` / `far:tray-arrow-up` | `bs:upload`（上传） |
| tray-full | `fas:tray-full` / `far:tray-full` | `bs:inbox`（） |
| tree | `fas:tree` / `far:tree` | `bs:tree`（） |
| triangle-exclamation | `fas:triangle-exclamation` / `far:triangle-exclamation` | `bs:exclamation-triangle`（警告三角） |
| trophy | `fas:trophy` / `far:trophy` | `bs:trophy`（奖杯） |
| truck | `fas:truck` / `far:truck` | `bs:truck`（） |
| truck-fast | `fas:truck-fast` / `far:truck-fast` | `bs:truck`（） |
| truck-front | `fas:truck-front` / `far:truck-front` | `bs:truck`（） |
| truck-medical | `fas:truck-medical` / `far:truck-medical` | `bs:truck`（） |
| truck-moving | `fas:truck-moving` / `far:truck-moving` | `bs:truck`（） |
| truck-pickup | `fas:truck-pickup` / `far:truck-pickup` | `bs:truck`（） |
| truck-ramp-box | `fas:truck-ramp-box` / `far:truck-ramp-box` | `bs:truck`（） |
| trumpet | `fas:trumpet` / `far:trumpet` | `bs:music-note`（音符） |
| tv | `fas:tv` / `far:tv` | `bs:display`（显示器） |
| tv-regular | `fas:tv-regular` / `far:tv-regular` | `bs:display`（显示器） |
| umbrella | `fas:umbrella` / `far:umbrella` | `bs:umbrella`（） |
| umbrella-beach | `fas:umbrella-beach` / `far:umbrella-beach` | `bs:umbrella`（） |
| umbrella-regular | `fas:umbrella-regular` / `far:umbrella-regular` | `bs:umbrella`（） |
| university | `fas:university` / `far:university` | `bs:bank`（银行） |
| unlink | `fas:unlink` / `far:unlink` | `bs:link`（） |
| unlink-regular | `fas:unlink-regular` / `far:unlink-regular` | `bs:link`（） |
| unlock | `fas:unlock` / `far:unlock` | `bs:unlock`（开锁） |
| up-down | `fas:up-down` / `far:up-down` | `bs:arrow-up-down`（） |
| up-down-left-right | `fas:up-down-left-right` / `far:up-down-left-right` | `bs:arrows-move`（） |
| up-right-and-down-left-from-center | `fas:up-right-and-down-left-from-center` / `far:up-right-and-down-left-from-center` | `bs:arrows-fullscreen`（） |
| upload | `fas:upload` / `far:upload` | `bs:upload`（上传） |
| user | `fas:user` / `far:user` | `bs:person`（单人） |
| user-check | `fas:user-check` / `far:user-check` | `bs:person-check`（人员对勾） |
| user-doctor | `fas:user-doctor` / `far:user-doctor` | `bs:person-badge`（人员徽章） |
| user-gear | `fas:user-gear` / `far:user-gear` | `bs:person-gear`（） |
| user-graduate | `fas:user-graduate` / `far:user-graduate` | `bs:mortarboard`（） |
| user-group | `fas:user-group` / `far:user-group` | `bs:people`（多人） |
| user-lock | `fas:user-lock` / `far:user-lock` | `bs:person-lock`（） |
| user-minus | `fas:user-minus` / `far:user-minus` | `bs:person-dash`（） |
| user-nurse | `fas:user-nurse` / `far:user-nurse` | `bs:person-badge`（人员徽章） |
| user-plus | `fas:user-plus` / `far:user-plus` | `bs:person-plus`（添加人员） |
| user-secret | `fas:user-secret` / `far:user-secret` | `bs:person`（单人） |
| user-shield | `fas:user-shield` / `far:user-shield` | `bs:person-shield`（） |
| user-tie | `fas:user-tie` / `far:user-tie` | `bs:person-badge`（人员徽章） |
| user-xmark | `fas:user-xmark` / `far:user-xmark` | `bs:person-x`（） |
| users | `fas:users` / `far:users` | `bs:people`（多人） |
| utensils | `fas:utensils` / `far:utensils` | `bs:cup`（） |
| van-shuttle | `fas:van-shuttle` / `far:van-shuttle` | `bs:bus-front`（） |
| vault | `fas:vault` / `far:vault` | `bs:safe`（保险箱） |
| vector-square | `fas:vector-square` / `far:vector-square` | `bs:square`（） |
| vial | `fas:vial` / `far:vial` | `bs:flask`（） |
| vials | `fas:vials` / `far:vials` | `bs:flask`（） |
| violin | `fas:violin` / `far:violin` | `bs:music-note`（音符） |
| virus | `fas:virus` / `far:virus` | `bs:virus`（） |
| volleyball | `fas:volleyball` / `far:volleyball` | `bs:trophy`（奖杯） |
| volleyball-regular | `fas:volleyball-regular` / `far:volleyball-regular` | `bs:trophy`（奖杯） |
| volume-high | `fas:volume-high` / `far:volume-high` | `bs:volume-up`（音量） |
| volume-low | `fas:volume-low` / `far:volume-low` | `bs:volume-down`（） |
| volume-off | `fas:volume-off` / `far:volume-off` | `bs:volume-mute`（） |
| volume-regular | `fas:volume-regular` / `far:volume-regular` | `bs:volume-up`（音量） |
| volume-xmark | `fas:volume-xmark` / `far:volume-xmark` | `bs:volume-mute`（） |
| walking | `fas:walking` / `far:walking` | `bs:person-walking`（） |
| wand-magic-sparkles | `fas:wand-magic-sparkles` / `far:wand-magic-sparkles` | `bs:magic`（） |
| wand-sparkles | `fas:wand-sparkles` / `far:wand-sparkles` | `bs:magic`（） |
| warehouse | `fas:warehouse` / `far:warehouse` | `bs:building`（大楼） |
| warning | `fas:warning` / `far:warning` | `bs:exclamation-triangle`（警告三角） |
| warning-regular | `fas:warning-regular` / `far:warning-regular` | `bs:exclamation-triangle`（警告三角） |
| water | `fas:water` / `far:water` | `bs:droplet`（水滴） |
| water-arrow-down | `fas:water-arrow-down` / `far:water-arrow-down` | `bs:cloud-arrow-down`（云端下载） |
| water-arrow-up | `fas:water-arrow-up` / `far:water-arrow-up` | `bs:cloud-arrow-up`（云端上传） |
| water-ladder | `fas:water-ladder` / `far:water-ladder` | `bs:droplet`（水滴） |
| water-regular | `fas:water-regular` / `far:water-regular` | `bs:droplet`（水滴） |
| wave-square | `fas:wave-square` / `far:wave-square` | `bs:activity`（脉搏） |
| waveform | `fas:waveform` / `far:waveform` | `bs:activity`（脉搏） |
| wheelchair | `fas:wheelchair` / `far:wheelchair` | `bs:person-wheelchair`（） |
| whiskey-glass | `fas:whiskey-glass` / `far:whiskey-glass` | `bs:cup`（） |
| whiskey-glass-regular | `fas:whiskey-glass-regular` / `far:whiskey-glass-regular` | `bs:cup`（） |
| wifi | `fas:wifi` / `far:wifi` | `bs:wifi`（无线） |
| wind | `fas:wind` / `far:wind` | `bs:wind`（） |
| wind-circle-exclamation | `fas:wind-circle-exclamation` / `far:wind-circle-exclamation` | `bs:cloud-fog2`（） |
| wind-warning | `fas:wind-warning` / `far:wind-warning` | `bs:cloud-fog2`（） |
| wine-glass | `fas:wine-glass` / `far:wine-glass` | `bs:cup`（） |
| wine-glass-regular | `fas:wine-glass-regular` / `far:wine-glass-regular` | `bs:cup`（） |
| won-sign | `fas:won-sign` / `far:won-sign` | `bs:currency-won`（） |
| worm | `fas:worm` / `far:worm` | `bs:bug`（） |
| wrench | `fas:wrench` / `far:wrench` | `bs:wrench`（扳手） |
| x | `fas:x` / `far:x` | `bs:x`（叉号） |
| x-ray | `fas:x-ray` / `far:x-ray` | `bs:activity`（脉搏） |
| xmark | `fas:xmark` / `far:xmark` | `bs:x`（叉号） |
| xmark-regular | `fas:xmark-regular` / `far:xmark-regular` | `bs:x`（叉号） |
| yen-sign | `fas:yen-sign` / `far:yen-sign` | `bs:currency-yen`（日元） |
| zap | `fas:zap` / `far:zap` | `bs:lightning-charge`（闪电） |
